[
	{
		"pattern": "HTML transform error: XSLT compile error",
		"reason": "Known XSLT issue from CdaParsingService. task-014 mitigated the noise on SendMail by switching MssanteHeaderService to CdaParseOptions.Metadata, but the pipeline d'enrichissement clinique still hits it. Root-cause fix tracked separately.",
		"since": "2026-04-27"
	},
	{
		"pattern": "Connection failed, host 127.0.0.1:5672",
		"reason": "RabbitMQ MassTransit retries during Aspire AppHost cold-start. Resolves once the rabbit container is fully ready.",
		"since": "2026-04-28"
	},
	{
		"pattern": "duplicate key value violates unique constraint \"IX_Mails_FolderPath_Uid_Unique\"",
		"reason": "Race between BackgroundImapService sync and EnrichEmailsSyncAsync — both insert the same (FolderPath, Uid). Fix candidate : UPSERT in MailRepository.AddNewMail. Tracked separately.",
		"since": "2026-04-27"
	},
	{
		"pattern": "Failed to append message to Sent folder",
		"reason": "MailKit FolderNotFoundException — the test mailbox uses 'Envoyés' / [Gmail]/Sent Mail / etc. depending on provider. ImapService.AppendToSentAsync should resolve via SpecialUse flag. Tracked separately.",
		"since": "2026-04-27"
	},
	{
		"pattern": "InvalidResponseHeaderRemoved",
		"reason": "Kestrel removes HTTP/1-only headers (Connection, Transfer-Encoding) from HTTP/2 responses. Logged by Kestrel itself. Cosmetic — should be cleaned in NotificationsController stream endpoint.",
		"since": "2026-04-27"
	}
]
