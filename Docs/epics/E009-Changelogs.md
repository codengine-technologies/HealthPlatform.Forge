# E009 — Changelogs ingénierie « Messagerie intelligente MSSante »

> **Audience** : équipes techniques, backlog, dette, audit qualité.
> **Document frère (vue produit / direction)** : [`E009-messagerie-securisee-sante.md`](./E009-messagerie-securisee-sante.md)
> **Dernière mise à jour** : 2026-07-28 (v1.53)
>
> **Continuité de l'historique** : les sauts de numérotation entre task-094,
> task-153 et task-175 ne sont **pas** un retard de documentation. Sur la plage
> **095 → 174**, une seule task appartient à E009 (**task-153**, « Répondre à
> tous ») ; les autres relèvent d'autres EPICs — **38 de E012** (client-mobile,
> miroir de client-angular), **21 de E014** (design system mobile Stitch), 2 de
> E015 (banc de charge), 2 de E013, et 10 sans EPIC déclaré. Elles sont donc
> hors périmètre de ce document par construction.
>
> Ce document concentre les détails ingénierie qui n'ont pas leur place dans la
> vue produit : task IDs, numéros de PR, versions NuGet, métriques tests,
> audits grep, décisions de refactor, métriques Sonar, file paths, limites
> différées. Le doc produit conserve la vision business et la conformité
> réglementaire ; les `task-XXX` y apparaissent en spine discret. Cette
> séparation suit la convention forge fixée le 2026-05-16
> (cf. `agents/technical-writer.md`).

---

## Historique détaillé des changelogs

<details>
<summary>📜 Toutes les versions v1.5 → v1.53 (triées de la plus récente à la plus ancienne)</summary>

**Changelog v1.53** *(entrée de rattrapage, rédigée le 2026-07-28 — la task est mergée depuis le 2026-07-17)* : fonctionnalité — task-153 (« Répondre à tous », Angular + mobile). **US frontend-only bi-repo** : `client-mobile` (pushable, **PR #55**, mergée `6211ecb`) + `client-angular` (code-only, PR TFS humaine) ; **`api-mail`/`dtos-mss` non touchés** (l'envoi passe par le `sendMail` existant, aucun changement de contrat). **Constat d'origine** : l'analyse différentielle du 2026-07-05 a montré que « Répondre à tous » manquait **dans les deux** clients (`mail-detail.component.ts` Angular ne pré-remplissait que l'expéditeur) — un manque **commun**, pas un écart de parité, d'où une US bi-repo sur une branche unique `feat/task-153-reply-all`. **Règle de calcul des destinataires** (spécification unique, deux implémentations TS) : **À** = `Reply-To` s'il existe sinon `From`, **+** tous les `To` d'origine **moins l'adresse de la boîte du praticien** (jamais d'auto-adressage) ; **Cc** = les `Cc` d'origine moins l'adresse du praticien ; **aucun `Cci` repris** (jamais divulgué) ; normalisation casse/espaces. Citation, objet « Re: » et threading RFC-5322 **réutilisent** la mécanique de « Répondre » déjà en place (`rfc5322-threading.utils.ts` côté Angular). **Visibilité conditionnelle** : l'action n'apparaît que si le mail d'origine compte **plus d'un correspondant après filtrage** — même règle sur les deux clients, donc pas de doublon d'action inutile sur un mail mono-correspondant. La logique est isolée dans un **util pur** (`reply-all-recipients.util.ts`) testé unitairement de façon identique sur chaque client. Libellés **FR en dur** (pas de ngx-translate côté MSS), `data-testid` sur la nouvelle action. **Tests** : mobile build 0 erreur / **522 tests** ; Angular MSS `nx test mss-lib` **312** + `nx lint mss-lib` (scope `tag:scope:mss`) 0 erreur. Code review **APPROVED** sur les 2 repos. **⚠️ Conflit d'intégration sémantique résolu en staging** : task-152 (#54) et task-153 (#55) ajoutaient toutes deux un champ `to?` à `ComposeRequest` avec des **types incompatibles** (`string[]` vs `MailAddressDto[]`) ; réconcilié dans `forge/staging-task-142-160-20260716` (674 tests verts) en renommant le champ `string[]` de task-152 en `toEmails` (`mail-event.service`, `mail-compose`, `ai-chat` + specs). Appliqué au merge du 2026-07-17 (`toEmails`/`to`/`cc`, consommateurs renommés, spec union). Couloir Ségur : médecine de ville — **hors DSR nouvelle**, complément d'une capacité d'émission déjà référencée (en-têtes MSSanté et libellé expéditeur inchangés, générés à l'envoi par le backend, tasks 001/009) ; journalisation PGSSI-S de l'envoi **héritée** du canal `api-mail` existant ; habilitations et authentification forte inchangées. Point d'attention consentement : si un destinataire d'origine est un patient MES, les garde-fous existants (opposition, blocage réponse) s'appliquent au compose pré-rempli comme à tout envoi — aucune règle nouvelle. Aucune règle Ségur déclarée (`**Closes RG**:`). Annexe C enrichie avec task-153. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.52** : sécurité / conformité — task-175 (Fuite inter-praticiens sur le flux SSE : le broker d'évènements mail est clé par dossier, pas par boîte). **US backend-only mono-repo `api-mail`** (**PR #126**) ; `dtos-mss` branche vide (aucun changement de contrat, pas de PR) ; **les trois frontends ne sont pas modifiés** (URL, noms d'évènements et forme du payload `SseMailEvent` inchangés — correctif invisible côté client hormis la disparition des évènements qui ne les concernaient pas). **Défaut corrigé** : `SseMailEventBroker` est un **singleton** dont la table d'abonnés (`ConcurrentDictionary<string, ImmutableArray<Subscription>>`) était indexée sur le **seul nom de dossier** (`INBOX`, `Sent`, …), identique pour tout le monde ; `PublishAsync(folder, …)` écrivait donc l'évènement dans le canal SSE de **tous** les praticiens connectés sur le même nom de dossier — sujet, expéditeur, contenu, documents CDA et `PatientInsMatricule` compris. L'isolation base-par-praticien qui protège le reste de l'API n'intervient pas : le fan-out a lieu **en mémoire, après** la lecture des données. Contraste interne : `SseNotificationBroker.Subscribe(string userEmail)` et `SseSyncProgressBroker` étaient déjà correctement clés par praticien — seul le broker mail avait été oublié. **Correctif — clé composite (boîte, dossier)** : `ISseMailEventBroker.Subscribe/PublishEmailsEnrichedAsync/PublishTagsUpdatedAsync/SubscriberCount` prennent la boîte propriétaire ; le bucket devient un `private readonly record struct MailboxFolderKey(MailboxEmail, Folder)` **normalisé à la construction** (trim + lowercase sur les deux composants), ce qui **conserve** l'insensibilité à la casse que l'ancienne clé tenait de son comparateur `StringComparer.OrdinalIgnoreCase`. `ISseMailEventSubscription` expose `MailboxEmail` ; la boucle CAS de `Remove`, `SubscriberCount` et le nettoyage de bucket sont portés sur la nouvelle clé (pas de bucket orphelin après déconnexion). **Provenance de l'identité** : `MailEventsController` s'abonne avec le claim JWT **`mssEmail` validé** — la même identité que les deux autres brokers, **contrat task-022 préservé** (toute query string `?email=` reste ignorée par construction) ; côté publication l'identité vient du **contexte de la boîte réellement synchronisée** et **jamais** d'un `IHttpContextAccessor` (l'enrichissement tourne sur des scopes de fond, hors requête HTTP) : `userContextInfo.Email` sur les 3 sites d'appel d'`ImapService`, `message.UserContext.Email` dans `AddNewMailConsumer` (motif déjà établi dans ce consumer, cf. `ConfigureUserContext`). Vérifié que `UserContextInfo.Email` est bien renseigné sur ces scopes (`BackgroundSyncManager` l'initialise, `BackgroundEnrichmentProcessor.CopyUserContext` le recopie sur chaque worker) — le fail-close ne coupe donc pas silencieusement les notifications de la sync de fond. **Fail-close** : une publication sans identité de boîte ne sert **personne** et journalise un `Warning`, au lieu de retomber sur la diffusion large que la task supprime. **Zéro donnée de santé au journal** : nouveau helper `MailboxPseudonym.Of` (`src/Application/Helpers/`, SHA-256 tronqué à 8 hex de l'adresse normalisée) — broker et notifier journalisent un jeton **pseudonymisé** stable (corrélation subscribe ↔ publishes possible) au lieu de l'adresse MSSanté ; aucun sujet, INS ni contenu. `[ExcludeFromCodeCoverage]` **retiré** de `SseMailEventBroker` (la classe est désormais testée). **Tests** : **23 unitaires** `SseMailEventBrokerTests` (cloisonnement `EmailsEnriched` **et** `TagsUpdated` entre deux boîtes sur le même dossier ; isolation entre dossiers d'une même boîte ; **non-sur-correction** — deux onglets de la même boîte reçoivent bien ; casse/trim ; fail-close ×4 avec assertion sur le `Warning` ; `Subscribe` rejette boîte/dossier vides ×6 ; **bucket physiquement retiré** au `Dispose` du dernier abonné — assertion par réflexion sur la table d'abonnés, un `ImmutableArray` vide résiduel comptant comme fuite mémoire par boîte ; `Dispose` idempotent ; **journal sans PHI ni adresse**) + **4 d'intégration** `MailEventsStreamIsolationIntegrationTests` (endpoint réel piloté à travers le graphe DI avec les **durées de vie de production** — broker singleton, notifier scoped résolu d'un **3ᵉ** scope comme l'enrichissement hors HTTP ; assertions sur les **octets réellement écrits dans le corps de réponse** de chaque praticien, pas sur des compteurs de broker) + 2 tests contrôleur (abonnement mail sur la boîte du claim, `?email=` ignoré ; pas d'abonnement mail sans `folder`). **Non-vacuité prouvée** : clé temporairement remise en « dossier seul » → **6 tests échouent** (`Publish_ToAnotherMailboxOnTheSameFolder_*`, `PublishTagsUpdated_ToAnotherMailboxOnTheSameFolder_*`, `SubscriberCount_IsScopedToTheMailbox`, `Dispose_LastSubscriberOfAMailbox_RemovesTheBucket`, 2/4 intégration), puis restaurée. Build 0 erreur / 0 avertissement ; suite complète **3169 ✓ / 0 ✗ / 16 ignorés** (suite AI, service externe absent). Flaky PDF export pré-existant (`MailExportServiceTests`/`MarkdownPdfRendererTests`, « font /F5 ») : sort par intermittence à 1851/1852 — **vérifié indépendant** (31/31 en isolation, échec identique sur le code sans les changements de la task). **Simplify** : 1 fix appliqué (`63430f8`) — chemin de comptage unique via une surcharge privée `SubscriberCount(MailboxFolderKey)`, double normalisation supprimée dans `Remove`. **Sonar** : 2 itérations — les 3 findings introduits (`csharpsquid:S103`, signatures allongées par le nouveau paramètre) **tous corrigés** (`3671229`) ; smells 6 → 3, dette 14 → 11 min, bugs/vulns/hotspots 0, ratings A/A/A, coverage 86.6 → 86.5 %. **Quality Gate `ERROR` non causé par cette PR** : unique condition en échec `new_violations = 3`, ces 3 violations étant `python:S1481` ×2 + `python:S3457` dans `tests/loadtest-k6/report.py` — fichier **absent du diff**, dernier commit `134647b` (task-174) **déjà sur `develop`**, remontées comme « new code » du fait de la baseline large de la new-code period ; non corrigées ici pour ne pas toucher un autre module (règle 6) ni bruiter la relecture d'un correctif de cloisonnement de données de santé (règle 5) → housekeeping dédié proposé. `conventions/csharp.md` alimenté : entrée **S103 — signature > 150 caractères ⇒ un paramètre par ligne** (4 occurrences). **Pistes relevées et écartées** (fichiers explicitement « Hors scope » de la task) : (1) les **3** brokers SSE dupliquent la même machinerie CAS + `ImmutableArray` → candidat `SubscriptionBroker<TKey, TPayload>` générique ; (2) `SseNotificationBroker`/`SseSyncProgressBroker` journalisent encore l'**adresse MSSanté en clair**, incohérence entre frères à portée conformité PGSSI-S méritant un arbitrage. Couloir Ségur : médecine de ville, vague **V2 — volet MSSanté** — **correctif de conformité** sur le cloisonnement des données de santé entre praticiens (exigences MSSanté et PGSSI-S confidentialité / contrôle d'accès), aucune nouvelle exigence adressée. Aucune règle Ségur déclarée (`**Closes RG**:`). **AIPD / notification CNIL à qualifier avec le DPO** — livrable **humain** explicitement distinct du correctif technique, hors checklist DOD (donc non bloquant pour la PR) : le défaut constitue une divulgation de données de santé à des tiers non autorisés, dont la portée réelle (environnements, fenêtre d'exposition, nombre de praticiens simultanés) reste à qualifier. Annexe C enrichie avec task-175. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.51** : optimisation (perf + UX) — task-094 (Optimiser le déplacement d'emails : UX ultra fluide + réduction des traitements serveur). **US backend + frontend (sans Blazor)** : `api-mail` (pushable, **PR #111**) + `client-angular` (code-only, PR TFS humaine) ; **`dtos-mss` non touché** (aucun changement de contrat — hors périmètre, branche vide, pas de PR). **Backend — move IMAP avec rekey DB en place** : nouveau `IMailRepository.RekeyMovedMailsAsync(sourcePath, targetPath, IReadOnlyList<MailUidRemap>, ct)` + record `MailUidRemap(OldUid, NewUid)` (`src/Application/Models/`). Après un MOVE IMAP UIDPLUS réussi, la (les) ligne(s) locale(s) sont **rekeyées en place** (`FolderPath + Uid` : `source/oldUid → target/newUid`) au lieu du cycle coûteux **suppression + ré-enrichissement** du background sync. Les **données enrichies sont préservées** (lien patient, documents médicaux, tags, contenu) car les lignes filles sont rattachées au `Mail.Id` immuable. **Atomique** (un seul `SaveChangesAsync` = une transaction), **idempotent** et **sûr en concurrence** : ligne source absente → skip ; ligne cible déjà présente (un sync concurrent a re-enrichi) → suppression de la ligne source périmée (évite la violation de l'index unique `(FolderPath, Uid)`). `ImapFolderService.MoveEmailAsync` (single) + `BulkMoveEmailsAsync` (bulk) câblent le rekey à partir de la map UIDPLUS (`UniqueIdMap` = `KeyValuePair<source,dest>`) via `TryRekeyMovedMailsAsync` **best-effort** : IMAP reste source de vérité, échec DB non fatal (le sync de fond reste le filet). **En cas d'échec IMAP, aucune mutation DB** (le rekey n'est appelé que dans la branche succès `newUid.HasValue`). Invalidation cache source/cible **inchangée** ; controller/API/DTO **inchangés**. Mock `Infrastructure.Mock` mis à jour (no-op). **Frontend (`client-angular`, code-only) — refresh ciblé du dossier destination post-drop** : nouveau `MailDestinationRefreshService.refreshAfterMove(targetPath, expectedMinCount)` (`features/mail/services/`) — refresh borné à **retries 300/1200/3000 ms** (token `MSS_DEST_REFRESH_DELAYS`, surchargeable en test) avec **arrêt anticipé** dès que le `count` serveur du dossier cible atteint le compte optimiste attendu, sinon abandon après la dernière tentative (le poll périodique 30 s reste le filet ; **pas de polling global agressif**) ; skip-to-next sur erreur de fetch. Nouvelle méthode d'état `MailStateService.applyFolderMetadataByPath(folder)` : applique `count/unread/uids/uidNext` frais à un dossier de la sidebar (et `selectedFolder` si c'est lui), **filtre tombstones** (ne ressuscite pas un delete optimiste) sans perturber la liste affichée. `mail-list.component.applyMoveSuccess` déclenche `refreshAfterMove(target.path, expectedMinCount)` après les MAJ optimistes ; **aucune régression** sur le move optimiste + rollback (`handleDropError`) ni sur le deferred-delete/undo (task-093). Token `MSS_DEST_REFRESH_DELAYS` (`core/tokens/`). **Tests** : backend `MailRepositoryRekeyTests` **7/7** (single success, bulk success, **préservation enrichie** content+meddoc+patient+tag, no-op source manquante, no-op remaps vides, **drop concurrence cible existante**, batch mixte) ; frontend `nx test mss-lib` **298/298** (10 task-094 : `mail-destination-refresh.service.spec` 4 — succès 1 tentative, retry jusqu'au seuil, abandon après dernière tentative, skip sur erreur, fake timers ; `mail-state.service.spec` +4 applyFolderMetadataByPath ; `mail-list.component.spec` +2 move→refresh / échec→pas de refresh). `dotnet build` api-mail 0 erreur/0 warning ; `nx build`/`nx lint mss-lib` (scope `tag:scope:mss`) 0 erreur (33 warnings pré-existants hors task-094). Full-suite api-mail : 2 échecs **pré-existants flaky** sans rapport (`MailExportServiceTests.BuildPdfWithoutAttachmentsOmitsAttachmentSection`, `ImapConnectionServiceIntegrationTests.ConnectAsyncWithCancellationShouldRespectTokenAsync`) — **passent en isolation**, confirmés flaky, **pas une régression task-094**. **Sonar** : skip (infra non provisionnée). **Simplify** : clean skip (forte réutilisation — rekey calque `DeleteMailsByUidsAsync`, état Angular calque `updateFolderMetadata` + réutilise `applyTombstonesToFolder`). Note testabilité : le chemin `ImapFolderService` move nécessite un vrai `ImapClient` MailKit (non mockable en isolation — cohérent avec l'absence de tests unitaires de move pré-existants) ; la logique neuve (rekey) est couverte au niveau repository. Couloir Ségur : médecine de ville — **optimisation perf/ergonomie d'un flux existant**, hors exigence DSR (aucun nouveau flux MSSanté/CI-SIS, journalisation/habilitations héritées). Aucune règle Ségur déclarée (`**Closes RG**:`). Annexe C enrichie avec task-094. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.50** : fonctionnalité — task-093 (Glisser-déposer des courriers dans la messagerie Angular : déplacement entre dossiers + suppression). **US Angular-only** (`client-angular` code-only, humain gère commit/push/PR TFS) ; **`api-mail`/`dtos-mss` non touchés** (geste UI au-dessus des endpoints existants `moveEmail`/`bulkMoveEmails`/`deleteEmail`/`bulkDeleteEmails` de `MssApiService` — aucune PR GitHub). **Approche** : drag & drop **natif HTML5** (lignes `mail-header` `draggable`, dossiers `mail-folder-item` `dragover`/`drop`) + service signaux `MailDragService` (le `DataTransfer` étant illisible pendant `dragover`, le type de dossier source est miroité en signaux pour valider la cible à la volée). **Matrice de règles** isolée dans une fonction pure `evaluateMailDrop` (`core/utils/mail-drop-rules.util.ts`, enum `MailDropOutcome` Forbidden/Move/Delete) : cibles autorisées **{Inbox, Custom, Junk, Trash}** ; `*→Sent/Drafts/All/Important/Tag` **interdit** ; `Sent`/`Drafts` → **Trash uniquement** ; `Trash→Junk` interdit ; `Trash→Inbox/Custom` = **restauration** (Move) ; no-op même dossier géré par comparaison de `path` (`MailDragService.canDropOn`). **Suppression = deferred-delete style Gmail** (`MailPendingDeleteService`) : dépôt sur Trash retire les lignes optimistiquement + toast « Annuler » ~6 s **sans appel backend** ; « Annuler » réinsère les lignes (`MailStateService.restoreMailsToList`) **sans** delete ; expiration / nouveau lot / teardown → **flush** réel (`deleteEmail` pour 1, `bulkDeleteEmails` pour N). Cela contourne le problème de **nouvel UID IMAP** au restore et fait que la **cascade backend** (lien patient + documents rattachés supprimés à la mise en Corbeille, **reconstruits à la restauration** — comportement voulu, décision PO 2026-06-16) ne part qu'**après** la fenêtre d'annulation. Déplacements (Move/restore) → `moveEmail`/`bulkMoveEmails` immédiats, mise à jour optimiste façon `bulkMoveToFolder` + rollback (reload) + snackbar `MSS_SNACKBAR_SHOW` sur erreur. Routage du drop via `MailEventService.emailsDropped$` (mail-folder-item → mail-folder-list → mail-list, composants frères ; les UIDs voyagent par `MailDragService`). **🔴 Bug attrapé au `/review`** (CHANGES REQUESTED → corrigé même cycle) : l'undo perdait **silencieusement** le courrier restauré — `removeMailFromList` posait un **tombstone 90 s** que `restoreMailsToList` ne levait pas, donc le prochain chargement (`filterTombstonedUids`, `mail-list.component.ts`) re-masquait l'UID restauré dans la fenêtre TTL. **Fix** : `MailStateService.clearTombstones(folderPath, uids)` appelé par `restoreMailsToList` + 3 tests de régression (lève le tombstone ; no-op dossier/uid inconnu ; delete→undo restaure ET lève le tombstone). **UI** : `mail-undo-toast` (composant `ui/`, FR en dur, `data-testid="mail-trash-undo"`), highlight cible allowed/forbidden (tokens `var(--ds-color-*)`), `data-testid` `mail-drag-row`/`mail-folder-droptarget`. **Tests** : `nx test mss-lib` **288/288** (53 task-093 : `mail-drop-rules.util.spec` 27, `mail-drag.service.spec` 9, `mail-pending-delete.service.spec` 11, `mail-undo-toast.component.spec` 3, `mail-state.service.spec` +3 tombstone-undo), `nx build mss-lib` 0 erreur, `nx lint mss-lib` (scope `tag:scope:mss`, base `origin/next`) 0 erreur (33 warnings pré-existants hors task-093). `mss:build:production` échoue sur `apps/mss/src/environments/environment.prod.ts` manquant = **gap WIP humain pré-existant** sur `feature/nova-rewriting-mss`, hors task-093 (aucun fichier task-093 ne référence un environment). **Sonar** : skip (api-mail non touché). **Simplify** : clean skip (forte réutilisation, fonction pure source unique de vérité ; redondance mineure non corrigée : double enregistrement teardown `MailPendingDeleteService`, inoffensif). Couloir Ségur : médecine de ville, **ergonomie de gestion de la boîte** — **hors exigence DSR** (geste UI au-dessus d'opérations existantes ; journalisation PGSSI-S, habilitations et cascade **héritées** du backend). Aucune règle Ségur déclarée (`**Closes RG**:`). Annexe C enrichie avec task-093. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.49** : sécurité — task-092 (Durcissement et vérification de la politique CORS, suivi SonarQube **S5122** / task-045). **US mono-repo `api-mail`** (dtos vide, pas de PR). CORS extrait de `Program.AddCorsPolicies` (supprimé) vers `CorsPolicySetup.AddMssCors` (`src/Api/Configuration`, testable, exécuté à l'identique de la prod). **`AllowAnyOrigin()` retiré du code** (la branche dev de task-045 l'utilisait encore) : la policy `MobileWeb` applique **toujours** une whitelist restreinte `WithOrigins(...)`. `ResolveAllowedOrigins` : whitelist configurée (`Cors:AllowedOrigins`, appsettings/env) si présente ; sinon **prod → liste vide = refus de tout cross-origin** (défaut sûr), **dev → allowlist localhost** (4200/8080/5000-5001, surchargeable) plutôt qu'ouverture à toute origine. Pas d'`AllowCredentials` (auth Bearer, pas de cookie). `app.UseCors(CorsPolicySetup.PolicyName)` applique la policy restreinte. **Tests** : `CorsPolicyResolveOriginsTests` 3 (api.tests : origines configurées prioritaires ; prod sans config → vide ; dev sans config → localhost, jamais `*`) + `CorsPolicyIntegrationTests` 3 (TestServer + `Origin` header : origine déclarée → `Access-Control-Allow-Origin` renvoyé ; origine non déclarée → aucun en-tête allow-origin = refus ; jamais de wildcard `*`). **Sonar** : skip (infra non provisionnée) — la suppression d'`AllowAnyOrigin` résout S5122 au prochain scan. Couloir Ségur : médecine de ville — exigence transverse PGSSI-S (restriction des origines d'accès à une API exposant des données de santé). Aucune règle Ségur REM spécifique. Annexe C enrichie avec task-092. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.48** : sécurité — task-091 (En-têtes de sécurité HTTP). **US mono-repo `api-mail`** (dtos vide, pas de PR). `SecurityHeadersMiddleware` (`src/Api/Middleware`) posé **en tête de `ConfigurePipeline`** (après `MapDefaultEndpoints`/Prometheus), écrit les en-têtes via `context.Response.OnStarting` — choix clé : survivre à la **ré-exécution de l'exception handler** (task-055) pour que même les réponses `problem+json` 4xx/5xx portent les en-têtes. Émet : `Content-Security-Policy` (défaut restrictif : `default-src 'self'` ; `frame-ancestors 'self'` ; `object-src 'none'` ; `base-uri 'self'` ; `form-action 'self'` ; `style-src`/`script-src 'self' 'unsafe-inline'` pour garder Swagger UI utilisable), `X-Frame-Options: SAMEORIGIN` (+ `frame-ancestors` = double barrière clickjacking), `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, et `Strict-Transport-Security` **uniquement si `Request.IsHttps`** (HSTS, `max-age` 1 an + `includeSubDomains`). `SecurityHeadersOptions` (section `SecurityHeaders`) rend CSP + le reste **configurables dev/prod** sans recompilation (valeur vide = en-tête désactivé). **Pas d'impact sur les SPAs** Blazor/Angular : la CSP des réponses JSON de l'API ne contraint pas les documents SPA (servis depuis une autre origine, qui consomment via fetch) ; la CSP protège surtout le HTML servi par l'API (Swagger). CSP est par ailleurs une **2ᵉ barrière anti-XSS** complétant task-088 (défense en profondeur). **Tests** : `SecurityHeadersIntegrationTests` 3 (TestServer minimal) : présence + valeur des 4 en-têtes systématiques (nosniff, SAMEORIGIN, Referrer-Policy, CSP `frame-ancestors`/`object-src`), HSTS présent sur requête `https://`, HSTS absent sur `http://` clair. **Sonar** : skip (infra non provisionnée). Couloir Ségur : médecine de ville — exigence transverse PGSSI-S (durcissement applicatif, anti-XSS/anti-clickjacking/anti-sniffing, transport strict). Aucune règle Ségur REM spécifique. Annexe C enrichie avec task-091. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.47** : sécurité / disponibilité — task-090 (Limitation de débit des requêtes API). **US mono-repo `api-mail`** (dtos vide, pas de PR). Middleware .NET `AddRateLimiter` + `app.UseRateLimiter()` placé **après `UseAuthentication`/`UseAuthorization`** (pour partitionner par identité PS authentifiée). **Partition** : `RateLimitingSetup.ResolvePartitionKey` → `user:{sub|NameIdentifier|preferred_username|name}` si authentifié, sinon `ip:{RemoteIpAddress}` (`ip:unknown` en dernier recours) — un appelant saturé n'affame jamais les autres. **Limiteur** : `FixedWindowRateLimiter` (global) ; dépassement → **429** via `limiter.OnRejected` qui écrit un `application/problem+json` (RFC 7807 : `status`/`title`/`detail`/`instance` + `traceId`) et pose l'en-tête **`Retry-After`** (depuis `MetadataName.RetryAfter`). **Configurable** sans recompilation via la section `RateLimiting` (appsettings/env) : `Enabled`, `PermitLimit`, `WindowSeconds`, `QueueLimit`, `SensitivePermitLimit`, `SensitiveWindowSeconds`. Politique stricte nommée **`sensitive`** exposée (`[EnableRateLimiting("sensitive")]`) pour endpoints sensibles. `Enabled=false` → `GetNoLimiter` (aucun throttling, nominal préservé). `RateLimitingOptions` + `RateLimitingSetup` extraits dans `src/Api/Configuration` (façon `ResponseCompressionSetup`) pour être testés à l'identique de la prod. PGSSI-S : seules des métriques techniques (partition/endpoint) seraient journalisées, aucune donnée de santé. **Tests** : `RateLimitingPartitionKeyTests` 4 (api.tests, internals visibles : identité PS → `user:sub`, utilisateurs distincts → partitions distinctes = isolation, anonyme → `ip:`, repli stable) + `RateLimitingIntegrationTests` 2 (TestServer minimal `WebApplication` + `UseTestServer` façon HealthEndpointsTests : sous quota → 200 ; dépassement → 429 + `Retry-After` + `application/problem+json`, fenêtre 60 s pour ne pas se réapprovisionner pendant le test). **Sonar** : skip (infra non provisionnée ; code selon dotnet-coding-rules). Couloir Ségur : médecine de ville — exigence transverse PGSSI-S (§ disponibilité / protection contre l'abus, DoS applicatif, force brute). Aucune règle Ségur REM spécifique. Annexe C enrichie avec task-090. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.46** : sécurité / confidentialité — task-089 (Blocage du contenu distant des courriers anti-pistage, inspiré du `show_images` de Roundcube). **US frontend-only** : `client-blazor` PR #62 (`awaiting-human-merge`) ; `client-angular` code-only (humain gère TFS) ; **`api-mail`/`dtos-mss` branches vides** (le blocage est un contrôle de confidentialité au moment du rendu, là où vit l'état "afficher à la demande, ce courrier uniquement" — pas de changement backend ni de contrat, pas de PR). **Approche** : neutraliser les ressources distantes (`src`/`background`/`srcset` http(s), `url()` CSS distant) **avant** l'injection dans l'iframe sandboxée existante (cohérent avec l'assainissement task-088 ; les deux composent : assainir XSS côté serveur puis bloquer le distant côté rendu). Ressources embarquées `cid:` et inline `data:` préservées (non http → non matchées). Le HTML distant est réécrit `src="http…"` → `data-mss-blocked-src="http…"` (l'URL est conservée pour le révéler) ; "Afficher les images" re-rend le HTML **original** (le front le détient) pour **ce courrier uniquement**, sans persistance ni mémorisation d'expéditeur ; reset à chaque courrier ouvert. Implémenté à l'identique dans les 2 fronts (pattern task-084). **Blazor** : helper pur `RemoteContentBlocker` (`Domain/Helpers`, regex avec lookbehind `(?<![\w-])` pour l'idempotence) ; `MailBodyComponent` — bannière au-dessus du conteneur shadow-DOM (les 2 sites de rendu, multi-doc + single-doc), `loadHtmlInShadowDom` reçoit bloqué/original selon `_showRemoteImages` ; clés `Localizer` `RemoteImagesBlocked`/`ShowImages` (en+fr), `data-testid` `remote-images-banner`/`show-remote-images-btn`. **Angular** (code-only, `feature/nova-rewriting-mss`) : util pur `remote-content.util.ts` (`core/utils`, même regex) ; `mail-body.component` — getter `mailBodyHtml` renvoie bloqué/original, getter `hasBlockedRemoteContent`, `showImages()` ; bannière FR en dur, tokens `var(--ds-color-*)`. **Tests** : Blazor `RemoteContentBlockerTests` 9 (image distante neutralisée — plus de `src=` live mais URL stashée, `cid:`/`data:` préservés, `background`/`srcset`/`url()` distant, contenu propre intact, idempotence) + `MailBodyRemoteContentTests` 2 bUnit (bannière affichée via `WaitForElement`, "Afficher les images" la masque), suite plugin 126/128 (2 skips pré-existants) ; Angular `remote-content.util.spec` 7 + `mail-body.component.spec` 3 (bannière affichée, révélation à la demande, pas de bannière sans distant), `nx test mss-lib` 233/233, `nx lint mss-lib` 0 erreur (fix prettier auto sur l'util ; fix hors-scope `problem-details.utils.ts` reverté). **Sonar** : skip (api-mail non touché). PGSSI-S : confidentialité — empêche l'accusé de lecture implicite (pixel traceur) et l'exposition IP/poste du PS ; option de journalisation de l'action "afficher les images" non retenue (sans contenu). Couloir Ségur : médecine de ville, exigence transverse de confidentialité. Aucune règle Ségur REM spécifique déclarée. Annexe C enrichie avec task-089. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.45** : sécurité — task-088 (Assainissement HTML des courriers anti-XSS, inspiré du sanitiseur `rcube_washtml` de Roundcube). **api-mail** PR #107 (`awaiting-human-merge`) ; **client-blazor** PR #61 (`awaiting-human-merge`) ; **client-angular** code-only (humain gère TFS) ; `dtos-mss` **vide** (`MailContentDto.BodyHtml` existait déjà → pas de cascade NuGet). **Backend** : `IHtmlBodySanitizer`/`HtmlBodySanitizer` — assainisseur **allowlist bâti sur AngleSharp 1.5.0** (déjà dépendance du repo). **HtmlSanitizer/Ganss écarté** : toutes ses versions (jusqu'à 9.0.892) épinglent `AngleSharp [0.17.1]` + `AngleSharp.Css [0.17.0]`, incompatibles avec l'AngleSharp 1.5.0 du repo (casserait MailExport/MarkdownPdfRenderer/AnnuaireSante), et il porte l'advisory NU1902 GHSA-j92c-7v7g-gj3f (traité en erreur par le repo). Politique : allowlist de tags de formatage (titres, listes, tableaux, liens, images, span/div/font…), tags inconnus *unwrappés* (texte conservé), tags actifs supprimés avec leur sous-arbre (`script/style/object/embed/iframe/form/input/svg/...`), attributs `on*` retirés, schémas d'URL `javascript:`/`vbscript:`/`data:` exécutables bloqués (`data:image/*` toléré sur `<img>`), CSS inline `expression`/`behavior`/`@import`/`-moz-binding`/`javascript:` neutralisé. Appliqué à **deux chokepoints** : `EmailBuildingService.BuildMailDtoAsync` (avant stockage, source de vérité) et `ImapService.GetEmailContentAsync` (avant exposition API — couvre les lignes DB antérieures à la mesure + le fetch IMAP frais). Service `Singleton` (stateless, thread-safe). **CDA non régressé** : le HTML CDA suit un chemin séparé (`MailMedicalDocumentDto.BodyHtml` via `CdaFormatTransformer`), non touché. PGSSI-S : aucun corps de courrier loggué. **Blazor** : `MailReadOnlyView` — corps rendu dans l'**iframe sandboxée** (`loadHtmlInShadowDom`, JS interop, conteneur à id unique) au lieu de `@((MarkupString)BodyHtml)` brut ; `MailBodyComponent` utilisait déjà l'iframe. **Angular** (code-only, `feature/nova-rewriting-mss`) : `mail-read-only-view` — corps rendu via `mss-medical-html-frame` (iframe blob déjà utilisée par `mail-body`) au lieu de `bypassSecurityTrustHtml` ; computed `bodyHtml` retourne désormais la string brute (assainie côté serveur), import `SafeHtml` retiré. **Tests** : api-mail `HtmlBodySanitizerTests` 12 (script retiré, `on*` retiré, `javascript:` retiré, `data:` bloqué vs `data:image` toléré, CSS expression neutralisé, iframe/object/embed retirés, unwrap tag inconnu, formatage préservé, idempotence) + `MailHtmlSanitizationIntegrationTests` 1 (courrier piégé stocké → exposition assainie via `GetEmailContentAsync`) + `EmailBuildingServiceTests`/`ImapServiceTests`/`ImapServiceDraftTagTests` adaptés (nouvelle dépendance ctor) ; Blazor `MailReadOnlyViewTests` 2 (corps routé vers conteneur sandboxé, repli texte) ; Angular `mail-read-only-view.component.spec` 2, `nx test mss-lib` 223/223, `nx lint mss-lib` 0 erreur. **Sonar** : skip (infra SonarQube non provisionnée — serveur injoignable/token absent ; code écrit selon `dotnet-coding-rules`). Couloir Ségur : médecine de ville — **exigence transverse PGSSI-S** (§ intégrité / protection contre le code malveillant). Aucune règle Ségur REM spécifique déclarée (`**Closes RG**:`). Annexe C enrichie avec task-088. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.44** : fonctionnalité — task-087 (Affichage du quota d'occupation de la boîte MSSanté, inspiré de l'indicateur de quota IMAP de Roundcube). **Full-stack** : `dtos-mss` PR #24 (`awaiting-human-merge`, **NuGet 327.0.0** — `322→327` api-mail, `298→327` client-blazor) ; `api-mail` PR #106 (`awaiting-human-merge`) ; `client-blazor` PR #60 (`awaiting-human-merge`) ; `client-angular` code-only (humain gère TFS). **Contrat** : `MailboxQuotaDto` (`UsedBytes`/`TotalBytes`/`UsagePercentage`/`Available`). **Backend** : `IMailboxQuotaService`/`MailboxQuotaService` lisent l'extension IMAP QUOTA (RFC 2087) — `IImapClientWrapper.GetMailboxQuotaAsync` (nouvelle méthode du wrapper) lit `Inbox.GetQuotaAsync()` et normalise la ressource STORAGE (KiB → bytes via `CurrentStorageSize`/`StorageLimit`) ; le service vérifie d'abord `ImapCapabilities.Quota` ; endpoint `GET api/v1/account/quota` ajouté à `AccountController` (réutilise `IImapConnectionService.ConnectAsync`). Dégradation : serveur sans QUOTA ou sans limite → `Available=false` (200) ; connexion KO → `UnavailableException` → **503 `application/problem+json`** via `GlobalExceptionHandler` (règle 12). PGSSI-S : log **du seul pourcentage** (EventId 3710/3711), aucune donnée de santé. **Blazor** : `MailboxQuotaFooterComponent` (jauge linéaire en pied de `FolderListComponent`, splitter passé `flex:1` pour loger le footer) — paliers `--mss-warning` ≥80 % / `--mss-danger` ≥90 %, état « Quota non disponible » ; `IMailboxQuotaService`/`MailboxQuotaService` (`HttpRequestService.GetAsync`, lit `ProblemDetails`) ; clés `Localizer` `MailboxQuotaUsed`/`MailboxQuotaUnavailable` (en+fr) ; `data-testid="mailbox-quota*"`. **Angular** (code-only, branche `feature/nova-rewriting-mss`) : `MailboxQuotaWidgetComponent` (`libs/mss/src/ui/mailbox-quota-widget/`, patron `SyncProgressWidget`, tokens `var(--ds-color-*)`, ambre/rouge `#f59e0b`/`#ef4444`, libellés FR en dur) embarqué en pied de `MailFolderListComponent` ; `MssApiService.getMailboxQuota()` ; modèle `mailbox-quota.model.ts` ; export barrel `ui/index.ts`. **Tests** : api-mail `MailboxQuotaServiceTests` 5 (connexion KO→503, pas de capability QUOTA→indispo, snapshot null→indispo, calcul %, arrondi 1 décimale) + `AccountControllerTests` +2 (200 dispo / 200 indispo) + `AccountControllerQuotaIntegrationTests` 2 (endpoint via DI, dispo + serveur sans QUOTA) ; Blazor bUnit `MailboxQuotaFooterComponentTests` 6 (dispo, indispo, échec service, ambre 80 %, rouge 90 %, normal) ; Angular `mailbox-quota-widget.component.spec.ts` 6, `nx test mss-lib` 221/221, `nx lint mss-lib` 0 erreur (33 warnings pré-existants hors scope ; fix lint hors-scope sur `problem-details.utils.ts` reverté), `nx build mss-lib` OK. **Sonar** : skip — infra SonarQube non provisionnée dans l'environnement (serveur injoignable, `SONAR_TOKEN` absent) ; code backend écrit selon `dotnet-coding-rules` (anti-dette). **Simplify** : clean skip (code neuf aligné sur les patterns existants). Couloir Ségur : médecine de ville, supervision de la boîte — **hors exigence DSR** (n'altère ni le transport MSSanté ni le contenu ; métadonnée de volumétrie). Aucune règle Ségur déclarée (`**Closes RG**:`). Annexe C enrichie avec task-087. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.43** : fonctionnalité — task-086 (Import/export du carnet d'adresses au format vCard `.vcf`, inspiré des actions `import.php`/`export.php` de Roundcube). **Full-stack** : `api-mail` PR #105 (`awaiting-human-merge`) ; `client-blazor` PR #59 (`awaiting-human-merge`) ; `client-angular` code-only (humain gère TFS) ; `dtos-mss` auto-incluse **vide** (design DTO compte-rendu local par repo → pas de cascade NuGet). **Backend** : `VCardSerializer` (reader/writer sans dépendance — écrit vCard 3.0, lit 3.0/4.0 tolérant : unfolding RFC 6350, unescape, property-name params, groupes `ITEM1.`) ; `IVCardService`/`VCardService` (export de tout le carnet ; import **anti-doublon** sur un snapshot `GetAllAsync` unique — match par RPPS puis par adresse MSSanté, update champ-à-champ + union d'adresses, jamais de clone ; une même adresse 2× dans le fichier → 1 création + 1 update) ; `VCardImportReport` (Created/Updated/Ignored + IgnoredReasons, DTO local `src/Application/Models`) ; `ContactController` → `GET api/v1/Contact/export/vcard` (`text/vcard` streamé via `StreamingFileResult`, pas de piège Kestrel sync-IO — leçon task-083) + `POST api/v1/Contact/import/vcard` (`[Consumes multipart/form-data]`, `IFormFile`, `FormatException`→`ValidationException`→**400 `ProblemDetails`** RFC 7807 via `GlobalExceptionHandler`, règle 12) ; PGSSI-S : log **volumétrie uniquement** (compteurs), aucun contenu de contact, pas d'ajout d'enum `AuditActionType` côté dtos-mss. **Blazor** : `ContactService.ExportVCardAsync` (`GetBytesAsync`) / `ImportVCardAsync` (`PostMultipartAsync`) ; `VCardImportReportDto` client-local ; `Contacts.razor` boutons Export (download JS `downloadFile`) / Import (`<InputFile>` caché stylé, garde 5 Mo), toast compte-rendu, `data-testid`, clés i18n EN+FR (`ExportVCard`/`ImportVCard`/`VCardImport*`). **Angular** (code-only, branche `feature/nova-rewriting-mss`) : `MssApiService.exportVcard()` (blob) / `importVcard(file)` (`FormData`) ; type `VCardImportReport` ; `mss-contacts.component` boutons Export/Import (icônes `export`/`import` user-interface), `<input type=file hidden>` via `viewChild`, message compte-rendu inline, libellés FR en dur (convention MSS, pas de ngx-translate), `data-testid`. **Tests** : api-mail 14 unit (serializer serialize/parse 3.0+4.0/malformé→FormatException/round-trip ; service create/update/anti-doublon RPPS+adresse/same-address-twice/malformé) + 5 controller (export streamé, import OK, no-file/empty/malformé→ValidationException) + 3 intégration Postgres Testcontainers (round-trip persistant, anti-doublon réimport, malformé) ; suites `api.tests` 510/510, `application.tests` vCard 14/14 (1 échec full-suite **pré-existant sans rapport** : `MarkdownPdfRendererTests.RenderHeadingPreservesText`, flaky PDF documenté). Blazor 2 bUnit (rendu des contrôles vCard + click Export appelle le service), suite plugin 101/101 (2 skips pré-existants). Angular 1 spec endpoints vCard (`mss-api.vcard.service.spec.ts`), `nx test mss-lib` 215/215, `nx lint mss-lib` 0 erreur, `nx build mss` (dev) OK. **Sonar** : delegué best-effort (api-mail touché — scan exclut `**/tests/**`, cf. v1.40). Couloir Ségur : médecine de ville, gestion du carnet d'adresses — **hors exigence DSR** (n'altère ni l'Annuaire Santé national ni le transport MSSanté). Aucune règle Ségur déclarée (`**Closes RG**:`). Annexe C enrichie avec task-086. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.42** : fonctionnalité — task-085 (Correcteur orthographique FR à la rédaction, inspiré du spell-check de Roundcube). **Front-only** : `client-blazor` PR #58 (`awaiting-human-merge`) ; `client-angular` code-only ; aucun backend (`dtos-mss` auto-incluse vide). **Exigence santé forte** : la correction s'opère **100 % localement dans le navigateur** (spellcheck natif), **aucun envoi** du contenu du courrier vers un service tiers cloud (risque d'exfiltration de données de santé). **Blazor** : `RadzenHtmlEditor` (corps) + `RadzenTextBox` (objet) reçoivent `spellcheck="true"` `lang="fr"` + `data-testid` (Radzen forwarde les attributs ; `lang`/`spellcheck` hérités par le contenteditable). **Angular** : l'éditeur **TipTap** du composant `html-editor` configuré via `editorProps.attributes` (`spellcheck:'true'`, `lang:'fr'`, `data-testid`) — appliqué au contenteditable ProseMirror ; la vue source HTML conserve `spellcheck="false"` (c'est du code). **Tests** : Blazor 1 test bUnit avec **rendu complet de `NewMailComponent`** (16 services substitués + Radzen + JSInterop loose) asservissant `spellcheck`/`lang` sur corps **et** objet, suite 100/100 ; Angular 1 spec asservissant `editor().options.editorProps.attributes`, suite mss-lib 213/213. Lint Angular 0 erreur. **Sonar** : skip (api-mail non touché). PGSSI-S : aucun (traitement local, le corps n'est jamais transmis ni loggué). Couloir Ségur : médecine de ville, confort de rédaction — hors DSR. Annexe C enrichie avec task-085.

**Changelog v1.41** : fonctionnalité — task-084 (Rappel de pièce jointe oubliée à l'envoi, inspiré du plugin `attachment_reminder` de Roundcube). **Front-only** : `client-blazor` PR #57 (`awaiting-human-merge`) ; `client-angular` code-only (humain gère TFS) ; aucun changement backend ni de contrat (`dtos-mss` auto-incluse vide). **Détection** : si le corps **ou** l'objet évoque une PJ (« ci-joint » + variantes, « pièce jointe » / « pièces jointes », « PJ » comme token isolé, « en annexe », « veuillez trouver ») **et** que 0 fichier n'est attaché → confirmation « Envoyer quand même ? » avant l'envoi. Liste d'expressions FR configurable, **insensible casse/accents** (normalisation NFD + strip diacritiques), **strip HTML** du corps. Implémentée à l'identique dans les 2 fronts (langages différents, même liste) : **Blazor** `AttachmentReminderDetector` (`Domain/Helpers`, pur) branché dans `NewMailComponent.OnValidSubmit` via `DialogService.Confirm` (4 clés i18n `AttachmentReminder*` en/fr) ; **Angular** util pur `attachment-reminder.util.ts` (`core/utils`) branché dans `mail-compose.send()` réutilisant le dialogue inline existant (`showConfirmDialog`/`pendingSendResolver`, nouveau signal `showMissingAttachmentConfirmation`, bloc template `data-testid=compose-missing-attachment-*`, libellés FR en dur convention MSS). Le flux d'envoi n'est pas altéré quand le PS confirme. **Tests** : Blazor 13 unitaires sur le détecteur (mention sans PJ → warn, avec PJ → pas de warn, sans mention → pas de warn, casse/accents, HTML, token PJ isolé), suite Blazor 112/112 ; Angular 13 unitaires sur l'util (mêmes cas), suite `mss-lib` 212/212. Lint Angular 0 erreur (33 warnings pré-existants hors scope). **Sonar** : skip (api-mail non touché). PGSSI-S : aucun (avertissement 100 % local au compositeur, le corps n'est jamais loggué — DOD santé). Couloir Ségur : médecine de ville, confort de rédaction — hors exigence DSR. Annexe C enrichie avec task-084.

**Changelog v1.40** : fonctionnalité — task-083 (Téléchargement groupé des pièces jointes en archive ZIP, inspiré du plugin `zipdownload` de Roundcube). **api-mail** intégré directement sur `develop` par l'humain (commit `5cd430a` forge + fix `9b6abbf`) ; **client-blazor** PR #56 (`awaiting-human-merge`) ; **client-angular** code-only (humain gère TFS) ; `dtos-mss` inchangé. **Backend** : nouvel endpoint `GET api/v1/mail/folders/{f}/emails/{id}/attachments/download/zip` → `application/zip`, archive produite **en flux jamais écrite sur disque**, chaque PJ tirée via `GetAttachmentStreamAsync` (réutilisé) et copiée entrée par entrée ; helper pur `AttachmentZipNameDisambiguator` (doublons : `resultat.pdf` → `resultat (1).pdf`, insensible casse, ordre préservé) ; aucune PJ → 404 `ProblemDetails` (règle 12) ; trace PGSSI-S en log structuré (nombre de PJ + id, **aucun nom de fichier** en Information) ; nom d'archive neutre `pieces-jointes-{id}.zip` (zéro donnée de santé en en-tête) ; réutilise `MailDto`/`AttachmentDto` (pas de changement de contrat). **🔴 Bug attrapé par l'humain** (`9b6abbf "Fix exception"`) : `ZipArchive.Dispose()` flush le central directory en **IO synchrone**, interdit sur `Response.Body` Kestrel (`AllowSynchronousIO=false`) → `InvalidOperationException` à l'exécution. Fix : bufferiser dans `FileBufferingWriteStream` (RAM puis fichier temp, accepte l'IO sync) puis drainer en async — mémoire bornée sans activer l'anti-pattern. **Le test controller de la forge ne l'a pas détecté** car écrit contre un `MemoryStream` (qui autorise l'IO sync), contrairement à Kestrel — d'où l'importance d'un vrai test d'intégration d'endpoint (TestServer/Testcontainers), à ajouter en suivi. **Frontends** : bouton « Tout télécharger (ZIP) » affiché à partir de **2 PJ réelles** (un ZIP d'1 fichier ferait doublon avec le download unitaire) — Blazor `AttachmentComponent` (`Localizer["DownloadAll"]`, `data-testid=attachment-download-all-zip`, JS interop `downloadFile`) ; Angular `mail-attachment` (`MssApiService.downloadAttachmentsZip`, libellé FR en dur convention MSS, `data-testid=download-all-attachments-zip`). **Tests** : backend 6 (désambiguïsation) + 3 (controller, ZIP réel composé/inspecté + 404) ; Blazor 4 bUnit (visible ≥2 / masqué <2 / masqué si IHE seul / clic déclenche ZIP), suite Blazor 99/99 ; Angular suite `mss-lib` 199/199. **Sonar** : Quality Gate **OK**, **0 issue new-code** (0 bug/vuln/smell/hotspot, coverage projet 84,8 %) — scan contourné sur un crash scanner `checkOverlappingBoundaries` dû à un caractère UTF-8 invalide dans un fichier de test pré-existant (`EmailManagementUseCaseTests.cs:779`), résolu en excluant `**/tests/**` de l'analyse (couverture `src/` préservée via OpenCover). **Lint Angular** : 0 erreur (33 warnings pré-existants hors scope). Couloir Ségur : médecine de ville, confort utilisateur — hors exigence DSR (n'altère pas le transport MSSanté ni le contenu). Tracé PGSSI-S : évènement « téléchargement groupé » (id mail + nombre de PJ). Annexe C enrichie avec task-083.

**Changelog v1.39** : qualité — task-082 (Campagne de couverture de tests backend, objectif initial 90 %) livrée sur `api-mail` (PR #104, `awaiting-human-merge`). `dtos-mss` : branche auto-incluse vide, pas de PR. **Cible rescopée 90 % → 85,4 %** (décision humaine au `/review` 2026-06-14) : figer le gain acquis plutôt que poursuivre une campagne chirurgicale de couverture de branches au rendement quasi nul. **Résultat : couverture globale 84,4 % → 85,4 % (+1,0 pt), +57 tests nets, zéro fichier de production modifié** (`git diff origin/develop...HEAD -- src/` vide, garde-fou DOD tests-only comme task-067). **Contenu (7 fichiers de test, +1134 lignes)** : `BaseEmbeddingProviderService` (troncature au-delà de `MaxEmbeddingCharacters` / succès / générateur qui throw → null) ; boucles hébergées à 0 % `BackgroundImapConnectionMaintenanceService` (cycle périodique, stop propre sur annulation, résilience à une passe qui throw) et `MailClientSessionCleanupService` (sweep concret expirant une session vs substitut non-concret → warning) ; happy-paths `ImapFolderService` (create avec/sans parent + namespace personnel, rename, delete avec/sans messages + self-heal mailbox déjà disparue, move/bulk-move avec trace d'audit, full cache hit sans connexion) ; branches d'entrée `ImapConnectionService` (domaine autodiscovery inconnu, PSC token vide, réutilisation connexion déjà authentifiée, + host-not-found/timeout/IO → `ResultStatus.Unavailable` 503 vs auth fail → `Error`) ; opérations draft/sent/tag/thread d'`ImapService` (UpdateEmailSummary, AppendToSent/Drafts, ReplaceDraft, DeleteDraft, Add/RemoveTag, GetEmailsByTag, GetMailsByTag streaming, GetThread) ; intégration Testcontainers `MailRepository` (12 tests verts vs vraie Postgres : read/unread, flag/unflag, GetMail header vs content, ResolveMailId, UpdateEmailSummary, MarkAsCancelled, DeleteMail, GetExistingUids/EnrichedUids, GetAttachment/UpdateAttachment). **Plafond technique constaté (it. 2)** : `BaseRepository`/`SemanticSearchRepository` **replanifiés hors task-082** — les méthodes ciblées étaient déjà couvertes transitivement (analyse OpenCover ligne par ligne : ~+2 lignes pour 12 tests), le restant pour 90 % (~1 080 lignes/conditions) est concentré dans des branches difficiles (handlers d'erreur, bootstrap per-tenant FluentMigrator de `BaseRepository`, chemins IMAP via extension methods MailKit non-mockables par NSubstitute). **🟡 Bug latent découvert** (hors périmètre tests-only, non corrigé) : `MarkReadReceiptSentAsync` — la colonne `ReadReceiptSentAt` est mappée `timestamptz` par le modèle EF (`EnsureCreated`) alors que le schéma de prod FluentMigrator la déclare `timestamp without time zone` (cf. task-078) ; le write `Kind=Unspecified` échoue sur le harnais d'intégration → discrépance EF↔migrations à corriger dans une task dédiée (touche le modèle EF). **Tests** : 94+502+353+1709 unitaires verts, intégration 237/254 (1 échec = IMAP cancel flaky pré-existant documenté, 16 skip AI/LLM réel). Build Release 0 erreur. **Sonar** : re-run complet `sonar-skill` délégué au Manual Test Plan humain ; chantier tests-only, aucune dette de production introduite. **Lint Angular** : skip (client-angular non touché). INS factices uniquement (DOD santé). Aucune règle Ségur déclarée (`**Closes RG**:`) — outillage qualité hors couloir Ségur. Annexe C enrichie avec task-082. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.38** : observabilité — task-081 (Timing par commande IMAP dans `GetFolderQuery`) livrée sur `api-mail` (PR #103, `awaiting-human-merge`). `dtos-mss` : branche vide, pas de PR. **Pourquoi** : listages de dossier lents en production (jusqu'à ~68 s sur `GetFolderQuery:INBOX`, audit Seq 2026-06-11 — `HoldTimeMs=68071` ≈ `ElapsedMs=68716`, donc temps passé à **exécuter** les commandes ; SEARCH 0 UID sur un INBOX de 354 messages → coût = round-trip IMAP serveur MSSanté, pas le volume) sans pouvoir nommer la commande coupable : le log n'émettait qu'un total agrégé après les 4 commandes. **Changements** : chaque commande de `GetFolderQueryAsync` (STATUS, SELECT/OPEN, SEARCH, CLOSE) chronométrée par un `Stopwatch` unique redémarré entre les commandes (mesures hors construction du DTO et hors boucle UIDs — attribution propre) ; 4 tags posés sur l'Activity `GetFolderQuery` existante : `imap.status_ms`, `imap.select_ms`, `imap.search_ms`, `imap.close_ms` (constantes `ActivityTags`) ; log `[ListFolder] IMAP SEARCH …` enrichi de `Timings: STATUS=…ms, SELECT=…ms, SEARCH=…ms, CLOSE=…ms` — ventilation requêtable dans Seq sans dépendre du tracing. Aucun changement de flux, d'ordre ni de mapping d'erreur ; pas d'`IProtocolLogger` MailKit (volume + risque de fuite). **Tests (test-first RED→GREEN)** : 4 — happy path (4 commandes reçues + 4 durées dans le log), tags capturés via `ActivityListener` sur la source Imap, folder non ouvrable (erreur propre, aucune commande émise), commande qui throw en milieu de séquence (erreur mappée, le chronométrage ne masque rien). Suites : 94+502+353+1667 unitaires verts, intégration 225/226 (seul échec = IMAP cancel flaky documenté). **Sonar** : Quality Gate **OK** après 1 itération — unique issue new-code S138 sur `CdaParsingService.ParseCdaDocument` (88 lignes, héritées du commit humain « Add metrics » squashé avec task-076/E011) → extraction `BuildCdaDocumentDto` (refactor pur, tests CDA verts) ; projet final 0 bug / 0 vuln / 0 smell / 0 hotspot, coverage **84,4 %**. **Lint Angular** : skip (client-angular non touché). Aucune règle Ségur déclarée (`**Closes RG**:`) — observabilité technique hors couloir Ségur (uniquement durées et compteurs dans les journaux, aucune donnée de santé — PGSSI-S). Annexe C enrichie avec task-081. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.37** : fix — task-078 (Courrier patient avec INS : crash `AddNewMail` sur DateTime Kind=Utc → timestamp sans time zone) livrée sur `api-mail` (PR #100, `awaiting-human-merge`). `dtos-mss` : branche vide, pas de PR. **Résout le 🔴 finding production gravé en v1.36 (task-067)** : le stamp `CreatedAt = DateTime.UtcNow` (Kind=Utc) du chemin de promotion « courrier patient » (`AddPatientMessageDocumentAsync`, déclenché pour `IsFromPatient` + `PatientInsMatricule`) violait la colonne `timestamp without time zone` — Npgsql levait `DbUpdateException` sur **tout** `AddNewMail` d'un mail patient Mon Espace Santé porteur d'une INS : mail non persisté, document « COURRIER » jamais créé, rattachement patient automatique (task-005) silencieusement cassé. **Fix minimal** : `CreatedAt = NormalizeUtc(DateTime.UtcNow)` (heure UTC conservée, Kind=Unspecified — convention du fichier), aucune migration. **Balayage du pattern (objectif 2)** : un seul autre site nu dans le fichier — `ReadReceiptSentAt` (accusés de réception, colonne `AsDateTime()`, même classe de bug latent) — corrigé à l'identique ; tous les autres sites passaient déjà par `NormalizeUtc`. **Tests (règle 1, test-first)** : 2 tests d'intégration Postgres Testcontainers (le bug n'est reproductible que contre le vrai type de colonne) écrits AVANT le fix, **RED confirmé avec l'exception Npgsql exacte** → GREEN : persistance sans `DbUpdateException` + document `COURRIER` portant l'INS (`CreatedAt.Kind == Unspecified`) ; 2e courrier du même patient → même `PatientId`, une seule fiche `MailPatients` (RG-E009-028/030 rétablies sur le chemin INS). INS factices uniquement. **Note suite** : `PatientUseCaseTests.GetMailsByInsWithPagination` échoue désormais **à l'identique sur develop pur** (vérifié par checkout) — test dépendant d'une boîte Gmail vivante dont le contenu a dérivé ; ajouté aux rouges environnementaux documentés (4e). **Note de séquencement** : PR #93 (task-070, E011) modifie `MailRepository.cs` en zone adjacente — hunks distincts, merge attendu propre. **Sonar** : Quality Gate OK premier scan, 0 issue new-code. **Lint Angular** : skip (client-angular non touché). Couloir Ségur : médecine de ville — correctif V2 d'un comportement déjà couvert (SC.MSS/UX.25 · ECO.3.1.1, SC.MSS/UX.31 · ECO.3.1.2), aucune exigence nouvelle. Annexe C enrichie avec task-078. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.36** : qualité & robustesse — task-067 (Campagne itérative de couverture de tests, pilotée par le `code-coverage-skill`) livrée sur `api-mail` (PR #89, `awaiting-human-merge`). `dtos-mss` : branche vide, pas de PR. **Résultat : couverture globale 77.4 % → 83.3 % (+5.9 pts), plancher DOD ≥ 83 % atteint ; Quality Gate SonarQube OK toutes conditions vertes pour la première fois** — la condition `new_coverage ≥ 80 %` (artefact de *new code period* documenté depuis task-055, qui faisait ERROR à chaque run) est résorbée par la campagne. **+69 tests nets, zéro fichier de production modifié** (`git diff -- src/` vide, garde-fou DOD). **Processus** : 5 itérations (analyse Sonar → sélection 1-10 classes par `uncovered_lines` → tests aux conventions repo xUnit+NSubstitute → re-scan) + une passe de clôture corrigeant les findings du scan 5 (xUnit2032), isolant une pollution inter-suites (embedding 3-dims semé visible par la recherche vectorielle d'une suite sœur → cleanup `finally`), retirant le test purge (TRUNCATE global vs DB Testcontainers partagée) et ajoutant 2 tests AiDiagnostics — passe documentée en transparence dans la PR pour l'arbitrage HAG. **Détail par itération** : (1) RedisSyncStateStore 0 %→couvert (18 tests : clés/TTL/NX, round-trips camelCase, résilience payloads corrompus, callbacks pub/sub), EmailSummaryService (chemins IA via `IChatCompletionService` substitué dans le kernel : génération, échec de persistance, forceRecalculate, extraction CDA, conversion HTML, troncature 15k), PendingActionService (SendMail success/échec/max-retries/sans-payload/skip PSC ; replay BiologyAck valide/corrompu/DocumentId vide), AiConversationService (StreamChatAsync : not-found, deltas+final+persistance assistant, GENERATION_ERROR, CANCELLED) → 79.1 % ; (2) MailRepository via Postgres Testcontainers (cycle de vie des tags manuels/IA/catégorie, dossiers tags, drafts CRUD+next-uid, snapshots uids/couverture, delete bulk, agrégation threads orphan-aware) → 80.7 % ; (3) SemanticSearchService (chemins embeddings via `IEmbeddingGenerator` substitué : scoring email+documents, dédup uids, recherche patient, résilience repo) + AddNewMailConsumer (pipeline complet via scoped-provider : embedding+docs médicaux+publishers+tagging+notification ; échec embedding propagé, échecs secondaires avalés) → 81.4 % ; (4) GetThreadAsync + persistance documents médicaux complets (SummaryItems, Attachments) + fix CA1861 → 81.6 % ; (5) chemin de promotion de contenu (mail header-only puis enrichi : UpdateExistingMailWithContent → AddPromotedMedicalDocument → SavePromotedContent → tags catégorie, idempotence) + MailMaintenanceController en intégration (downcast `DataContext` documenté non-unit-testable depuis task-043 : list-emails, email-details + NotFoundException) → 82.4 % ; clôture : AiDiagnosticsController (inventaire embeddings, debug vector-search avec distances cosinus sur embedding semé) → **83.3 %**. **🔴 Finding production** (non corrigé — task tests-only) : bug latent `AddPatientCourrierDocument` (MailRepository) — `CreatedAt = DateTime.UtcNow` (Kind=Utc) dans une colonne `timestamp without time zone` → `DbUpdateException` sur **tout** `AddNewMail` d'un mail patient portant une INS (`IsFromPatient` + `PatientInsMatricule`) ; task de fix dédiée recommandée (1 ligne + test de non-régression). **Fixtures** : INS factices uniquement (90000000000000x), domaines d'exemple — aucune donnée de santé réelle (DOD santé). Suite verte hors les 3 rouges pré-existants documentés ; builds validés en Release (lock Debug par l'API de dev, précédent task-024). **Lint Angular** : skip (client-angular non touché). Aucune règle Ségur déclarée — outillage qualité hors couloir Ségur. Annexe C enrichie avec task-067. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.35** : robustesse & observabilité — task-058 (Durcissement du `MailClientSessionManager` : fuite de sémaphores + boucle de cleanup résiliente) livrée sur `api-mail` (PR #88, `awaiting-human-merge`). `dtos-mss` : aucun changement de contrat → branche vide, pas de PR. **Finding B (🔴 fuite mémoire)** : les sémaphores `_imapFolderLocks`/`_emailFetchLocks` n'étaient nettoyés que sur logout **explicite** (`RemoveSession` → `CleanupLocksForSession`) ; `CleanupExpiredSessions` retirait la session de `_sessions` **sans jamais** récupérer ses locks — pour chaque session **expirée** (cas majoritaire : déconnexion sans logout), les entrées `SemaphoreSlim` restaient à vie → fuite monotone proportionnelle aux couples (utilisateur × dossier) vus depuis le boot du pod, jusqu'à OOM. **Désormais** : `CleanupExpiredSessions` appelle `CleanupLocksForSession` pour chaque session expirée (clé `{email}_{clientSessionId}` splittée sur le dernier `_`) ; `CleanupLocksForSession` nettoie **les deux** dictionnaires (correction de l'oubli symétrique sur `_emailFetchLocks`) — les fetch locks étant **partagés** entre sessions navigateur d'un même praticien (clé `fetch:{email}:{folder}`, sans sessionId), ils ne sont récupérés que quand la **dernière** session de l'email disparaît (garde `HasActiveSessionsForEmail`, pas de `Dispose` sous un holder actif). Nettoyage APRÈS `TryRemove` (nuance vs lettre du DOD « avant », documentée) : requis pour que le garde ne compte plus la session expirée — même ordre que le chemin logout existant. **Finding C (🟠 boucle tuable)** : dans `RunCleanupTaskAsync`, le `try/catch (Exception)` enveloppait le `while` entier — une seule exception inattendue de `CleanupExpiredSessions()` tuait la tâche périodique **pour toute la vie du pod** (plus aucun nettoyage → accumulation silencieuse). **Désormais** : try/catch **à l'intérieur** du while (sweep raté = `LogError` + continue), sortie propre sur annulation (catch `OperationCanceledException` autour du `Task.Delay`, log Information au shutdown), cadence exposée en `internal CleanupInterval` (1 min prod inchangé, ms en test). **Hooks de test internes** (sanctionnés par le DOD) : `FolderLockCount`, `EmailFetchLockCount`, `GetSessionForTests`, `MailClientSession.ForceExpire()`, `CleanupExpiredSessions` passé `virtual`. **Tests** : `MailClientSessionManagerCleanupTests` — 6 tests test-first (RED compile → GREEN) : expiration→reclaim session+folder+fetch locks, fetch locks préservés tant qu'une session sœur du même email vit (folder locks bien récupérés), sessions non expirées intactes, logout explicite récupère les deux dicts, la boucle survit à un sweep qui throw (2e sweep observé via `TaskCompletionSource`), sortie propre sur cancel. Application 1477/1477. **Review — notes non-bloquantes** : (1) `Dispose` d'un folder lock potentiellement encore détenu = pattern pré-existant du chemin logout, risque inchangé ; (2) TOCTOU étroit si une nouvelle session du même email apparaît pendant le reclaim des fetch locks — impact borné à une requête, dictionnaire repeuplé. **Note environnementale** : validation build/tests en **Release** (binaires Debug de `src/Api` verrouillés par l'API de dev en cours d'exécution — précédent task-024 ; `src/Api/` non touché). **Sonar** : 0 violation / 0 hotspot new-code ; diff intégralement couvert (`MailClientSession.cs` 0 ligne new non couverte, 94.3 % ; les 8 lignes « new » non couvertes du manager = handler timeout task-024, legacy classé « new » par la baseline large, hors périmètre) ; dette legacy nulle (ratings A/A/A). **Lint Angular** : skip (client-angular non touché). Aucune règle Ségur déclarée (`**Closes RG**:`) — robustesse interne plateforme, hors couloir Ségur (réduit le risque d'épuisement mémoire, disponibilité HDS). Annexe C enrichie avec task-058. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.34** : robustesse & observabilité — task-057 (Suppression des `new HttpClient()` OCSP — épuisement de sockets) livrée sur `api-mail` (PR #87, `awaiting-human-merge`). `dtos-mss` : aucun changement de contrat → branche vide, pas de PR. **Pourquoi** : `OcspValidationService` instanciait `new HttpClient()` à chaque requête OCSP (2 sites : envoi de la requête OCSP + téléchargement du certificat issuer) — épuisement de ports TCP sous charge (`TIME_WAIT`), contournement du resilience handler global (`ConfigureHttpClientDefaults`) et angle mort de l'instrumentation HttpClient OpenTelemetry. Le pattern correct existait déjà dans le même domaine (`CrlValidationService` + client nommé `"CrlClient"`). **Changements** : (1) `OcspValidationService` injecte `IHttpClientFactory` (primary ctor), les 2 `new HttpClient()` remplacés par `CreateClient("OcspClient")` (const `OcspClientName`) — **iso-comportement** : header `application/ocsp-request`, parsing `OcspResp`, logique de validation inchangés ; (2) `DependencyInjection.AddApi` : `services.AddHttpClient("OcspClient")` (miroir `CrlClient`) — les requêtes OCSP passent désormais par le resilience handler par défaut (retry/circuit-breaker/timeout) et l'instrumentation OTel HttpClient ; (3) `[ExcludeFromCodeCoverage]` retiré — la raison « untestable without responder mock harness » est caduque : harness responder construit dans les tests (CA + certificat feuille portant une extension AIA réelle via `X509AuthorityInformationAccessExtension`, réponses OCSP DER signées par la CA de test via BouncyCastle `BasicOcspRespGenerator`/`OCSPRespGenerator`, stub `HttpMessageHandler` derrière la factory mockée). **Tests** : `OcspValidationServiceTests` réécrits test-first (RED ctor → GREEN) — 12 tests : good→`Success(true)`+cache, revoked→`Success(false)` (un certificat révoqué reste rejeté — DOD santé), HTTP 500/timeout (`TaskCanceledException`)/erreur réseau issuer→`Error` contrôlée, statut responder `tryLater`→`Success(false)`, cache hit→court-circuit sans HTTP ni `CreateClient`, issuer en cache→POST OCSP seul, contrat client nommé (GET+POST via `"OcspClient"`), AIA sans URL OCSP / sans URL issuer / cert sans AIA→`Error`. **Piège harness documenté** : NSubstitute auto-retourne un tableau **vide** (pas null) pour `Get<byte[]>` → faux « cache hit » avec 0 byte et `CryptographicException: ASN1 corrupted data` ; fix épinglé `.Returns((byte[]?)null)` dans le ctor du test. **Sonar (Phase 1 active)** : 0 violation / 0 hotspot new-code ; couverture new-code du fichier comblée en 2 commits de tests dédiés (`d7e54f6`, `4494a49`) : `OcspValidationService.cs` 86.2 % → 92.8 % → **95.8 %**, `new_uncovered_lines` 5 → 0 (cible 95 % atteinte) ; dette legacy nulle (0 bug / 0 vuln / 0 smell, ratings A/A/A, duplication 0.6 %, coverage projet 77.4 %). Le `new_coverage` global (74 %) reste l'artefact de *new code period* projet — accepté best-effort. Suite verte hors les 3 rouges pré-existants documentés. **Lint Angular** : skip (client-angular non touché). Aucune règle Ségur déclarée (`**Closes RG**:`) — durcissement technique de la couche validation de certificat, hors couloir Ségur (le résultat de validation de la chaîne de confiance TLS MSSanté est iso). Annexe C enrichie avec task-057. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.33** : robustesse & observabilité — task-056 (Sondes de santé readiness/liveness en production avec dépendances) livrée sur `api-mail` (PR #86, `awaiting-human-merge`). `dtos-mss` : aucun changement de contrat → branche vide, pas de PR. **Pourquoi** : dans `DependencyInjectionExtensions.MapDefaultEndpoints`, les routes `/health` et `/alive` n'étaient mappées que sous `app.Environment.IsDevelopment()` — **aucune sonde exposée en production**, l'orchestrateur (Aspire / Kubernetes) ne pouvait détecter ni deadlock (liveness) ni dépendance indisponible (readiness) ; et le seul check enregistré était `"self"`. **Changements** : (1) garde `IsDevelopment()` supprimé — `/health` (readiness, `Predicate` tags `ready`) et `/alive` (liveness, `Predicate` tags `live`) mappés dans tous les environnements, avec `.AllowAnonymous()` requis par la `FallbackPolicy` d'auth globale (task-021) ; writer par défaut conservé = **statut agrégé seul** (`Healthy`/`Unhealthy`), aucun détail de check / connection string / version ne sort ; note de déploiement gravée en commentaire : endpoints anonymes **réservés réseau interne**, jamais exposés via l'ingress public ; (2) `AddDefaultHealthChecks` enrichi de 2 checks `ready` (timeout 5 s, enregistrement conditionnel à la présence de la connection string pour un boot honnête en environnement partiel) : **PostgreSQL** via `AddNpgSql` sur `MSS-MAIL-CONNECTIONSTRING` (connexion serveur — jamais une base tenant) et **Redis** via `AddRedis(sp => sp.GetRequiredService<IConnectionMultiplexer>())` (multiplexer Aspire `mss-mail-redis`) ; (3) **RabbitMQ** (`mss-mail-rabbitmq`) : couvert par le bus health check **auto-enregistré par MassTransit** (tags `ready`+`masstransit`) — pas de connexion de sonde dédiée redondante, contrat épinglé par le test `MassTransit_RegistersBusHealthCheck_TaggedReady` (rendra loud toute régression de tagging à un upgrade MassTransit) ; (4) packages `AspNetCore.HealthChecks.NpgSql` + `AspNetCore.HealthChecks.Redis` (9.0.0, famille déjà pinnée) référencés par le csproj Api ; lock files resynchronisés (`src/AppHost/packages.lock.json` -1400 lignes — drift pré-existant sur develop, régénéré par restore). **Exclusion tracing** : filtre OpenTelemetry existant inchangé (`/health` et `/alive` hors traces). **Tests** : `tests/mss.mail.integration.tests/Health/HealthEndpointsTests.cs` — 6 tests d'intégration TestServer en environnement **Production** via les extensions réelles (test-first RED 5/6 → GREEN) : `/alive` 200 `Healthy`, `/health` 200 quand checks `ready` sains, `/health` **503** quand une dépendance `ready` est down (check factice) avec `/alive` restant 200, leak-guard (body = statut agrégé même quand la description du check porte une connection string), `/alive` insensible aux deps down, contrat MassTransit ready-tag. Connection strings blanked dans le harness pour neutraliser l'environnement de la machine. Suite api-mail verte hors les 3 rouges pré-existants documentés (middleware DB-name Release, IMAP cancel flaky, MailExport PDF flaky — 2/3 verts en isolation). **Sonar** : new code 0 violation / 0 hotspot, dette legacy nulle (0 bug / 0 vuln / 0 smell, ratings A/A/A, duplication 0.6 %) ; `new_coverage` 74 % = artefact de *new code period* projet (legacy classé « new ») — le fichier touché a `new_uncovered_lines = 0` (classe `[ExcludeFromCodeCoverage]`, comportement couvert par les tests d'intégration). **Lint Angular** : skip (client-angular non touché). **Note PO** : la task déclare `**EpicTitle**: Robustesse & observabilité de la plateforme` divergent du titre E009 existant — résolution de slug standard (le fichier existant prime), à clarifier si un EPIC dédié observabilité était voulu. Aucune règle Ségur déclarée (`**Closes RG**:`) — infra/observabilité plateforme hors couloir Ségur. Annexe C enrichie avec task-056. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.32** : robustesse & observabilité — task-059 (Uniformisation du path `Result` sur `ProblemDetails` RFC 7807) livrée sur `api-mail` (PR #79, `awaiting-human-merge`). `dtos-mss` : aucun changement de contrat → branche vide, pas de PR. **Keystone post-task-055** : le dernier format d'erreur non conforme restant était le path `Ardalis.Result` — `ResultExtensions.ToActionResult` (`src/Api/Helpers/ResultModelExtensions.cs`) sérialisait encore les échecs en `ErrorResponse(Success, Message)` legacy. **Changements** : (1) les 2 surcharges `ToActionResult` produisent désormais un `ObjectResult` portant un `ProblemDetails` (`application/problem+json`) : `status` / `title` (via `ReasonPhrases.GetReasonPhrase`, mêmes libellés que `GlobalExceptionHandler`) / `detail` / `type` (`httpstatuses.io`) + extensions `traceId` (`Activity.Current?.Id ?? TraceIdentifier`) et `instance` (`METHOD /path`) — même enrichissement que la customisation `AddProblemDetails` de Program, stampé dans l'extension car ce path est sérialisé par MVC, pas par `IProblemDetailsService` ; (2) posture anti-fuite alignée task-055 : fallback 500 → `detail` générique (le message brut du service ne part que dans les logs via `LogServerFailure`), statuts typés (404/401/403/400/409/503) → message développeur sûr, `Result.Invalid` → `ValidationErrors` (joints « ; »), statut typé sans message → fallback client générique ; (3) mapping par type task-055 (`MapStatusCode`) inchangé ; `LogServerFailure` (Error log sur tout 5xx Result-mapped) préservé ; (4) record legacy `ErrorResponse(bool Success, string Message)` **supprimé** — audit grep : 0 référence dans `src/` et `tests/` ; (5) `[ExcludeFromCodeCoverage]` retiré de la classe, garde-fous null intestables sur `Errors`/`ValidationErrors` retirés (contrat Ardalis : collections jamais null). **Effet de levier** : tous les controllers au pattern `Result` émettent du `problem+json` sans modification individuelle — les tasks 060–065 n'ont plus qu'à retirer le boilerplate try/catch et les `[ExcludeFromCodeCoverage]`. Frontends inchangés : les parsers task-055 (Blazor `ProblemDetailsParser`, Angular `mapProblemDetailsToMessage`) consomment déjà le nouveau format (fallback legacy conservé). **Tests** : `ResultExtensionsTests` réécrits test-first (RED 26/28 → GREEN) — 30 tests : body `ProblemDetails` + content-type, leak-guard 500 (le `detail` n'écho jamais le message brut), théorie de mapping 6 statuts (status + title + type), `ValidationErrors`, précédence `Activity` sur `traceId`, `instance`, path logger de `LogServerFailure` (NSubstitute), bare-controller safety. Suite api-mail verte hors (1) **échec pré-existant documenté** `UserContextEnricherMiddlewareTests.AuthenticatedUserResolved_EmitsEventId3724` (rouge sur develop propre depuis task-055) et (2) flaky de parallélisation `ImapConnectionServiceIntegrationTests.ConnectAsyncWithCancellationShouldRespectTokenAsync` (+1 occurrence `MailExportServiceTests.BuildPdfWithoutAttachmentsOmitsAttachmentSection`), tous vérifiés verts en isolation. **Sonar (new code)** : `new_violations` 1→0 (CA1859 — type de retour concret `ObjectResult`), `new_bugs`/`new_vulnerabilities` 0, hotspots reviewed 100 %, `ResultModelExtensions.cs` new_coverage **95.7 %** (lignes 100 %, 0 ligne non couverte) ; projet : code_smells 0, ratings A/A/A, duplication 0.6 %. Le `new_coverage` global (72.5 %) reste l'artefact de *new code period* projet (~28k lignes legacy classées « new »), hors périmètre — accepté best-effort. **Lint Angular** : skip (client-angular non touché ; le WIP non commité dans `Client/Angular/` est l'héritage task-055/humain). Aucune règle Ségur déclarée (`**Closes RG**:`) — refactor technique transverse hors couloir Ségur (robustesse/observabilité plateforme). Annexe C enrichie avec task-059. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.31** : robustesse & observabilité — task-055 (Harmonisation de la gestion d'erreurs API via `ProblemDetails` RFC 7807) livrée sur `api-mail` (PR #78, `awaiting-human-merge`), `client-blazor` (PR #55, `awaiting-human-merge`) et `client-angular` (mode code-only, uncommitted sur `feature/nova-rewriting-mss`). `dtos-mss` : aucun changement de contrat (ASP.NET `ProblemDetails` est un standard) → branche vide, pas de PR. **Pourquoi** : les 25 controllers V1 répétaient un triple `try/catch` boilerplate (~93 `catch (Exception)`, ~66 `catch (OperationCanceledException)`, ~97 retours 500 codés en dur) avec trois formats d'erreur incohérents (`ErrorResponse(Success, Message)`, string brute `"Internal server error."`, string métier) et un mapping HTTP par **heuristique de mots-clés** fragile (`ResultExtensions.MapStatusCode` scrutait `token`/`connection`/… dans le message). **Changements backend** : (1) nouveau **`GlobalExceptionHandler`** (`src/Api/ErrorHandling/`, `IExceptionHandler` .NET 10) — mapping **par type** : `OperationCanceledException`→499, `ValidationException`→400, `NotFoundException`→404, `ConflictException`→409, `UnavailableException`/`OfflineModeException`→503, fallback→500 ; le `detail` exposé est **générique** pour 5xx/499 (jamais de stack trace, message d'exception système brut, ni donnée de santé) tandis que les exceptions métier typées portent un message développeur sûr ; (2) 4 exceptions métier typées dans `src/Application/Exceptions/` (`NotFoundException`, `ValidationException`, `ConflictException`, `UnavailableException`) ; (3) `Program.cs` : `AddProblemDetails` (customisation injectant `traceId` via `Activity.Current?.Id ?? TraceIdentifier` + `instance`) + `AddExceptionHandler<GlobalExceptionHandler>` + `app.UseExceptionHandler()` placé **en aval** de `RequestLoggingMiddleware` (ce dernier ré-emballe toute exception propagée en `InvalidOperationException` ; placer le handler en amont aurait cassé le mapping par type) ; (4) `ResultExtensions.MapStatusCode` réduit à un mapping strict par `ResultStatus` (heuristique de mots-clés `IsAuthenticationError`/`IsConnectionError` supprimée, fallback 500) ; (5) **`BiologyController` + `ContactController`** migrés : try/catch boilerplate retiré, `[ExcludeFromCodeCoverage]` retiré, not-found→`NotFoundException`, input invalide→`ValidationException`. **Changements Blazor** : nouveau `ProblemDetailsParser` (`Src/Modules/Mss/Application/Services/`) qui lit `problem+json` (`detail` → `title`) avec fallback gracieux vers l'ancien `ErrorResponse(message)` puis `null` ; `HttpRequestService.NotifyErrorFromResponseAsync` passe en sémantique **status-first** (401→`NotifyAuthenticationError`, 503→`NotifyConnectionError`, 404 silencieux, autres 4xx/5xx→message du ProblemDetails ou label générique) ; suppression de l'ancien `ErrorResponseDto(Success, Message)` ; `ExtractErrorMessageFromResponseAsync` rendu `static` (S2325). **Changements Angular** (code-only) : modèle partagé `ProblemDetails` (`libs/mss/src/core/models/`, généralise `DmpProblemDetails`) + utilitaire `mapProblemDetailsToMessage` / `isProblemDetails` (`libs/mss/src/core/utils/`) mappant `HttpErrorResponse.error` typé ProblemDetails vers un message FR (detail → title → message générique par status). **Tests** : api-mail — `GlobalExceptionHandlerTests` (1 test/branche 499/400/404/409/503/500 + garde anti-fuite vérifiant que le `detail` 500 n'écho jamais le message d'exception) + `GlobalExceptionHandlerIntegrationTests` (TestServer in-process sans Docker : exception non gérée → 500 `problem+json` + `traceId` sans fuite, `NotFoundException` → 404, `OperationCanceledException` → 499) + `BiologyControllerTests` + `ContactControllerTests` (33 tests couvrant les 2 controllers dé-`[ExcludeFromCodeCoverage]`) + `ResultExtensionsTests` mis à jour ; 67 tests task-055 verts (domain 94 / infra 322 / app 1462 / api 194-sur-195). Blazor `ProblemDetailsParserTests` (problem+json / legacy / vide-illisible) — 95/97 (2 skip historiques) / 0 failed. Angular `mss-lib` 198/198, lint MSS 0 erreur. **⚠️ Test pré-existant en échec NON lié** : `UserContextEnricherMiddlewareTests.AuthenticatedUserResolved_EmitsEventId3724_WithExpectedAnonymisedFields` (anonymisation) échoue déjà sur `develop` propre (vérifié branche stashée) — hors scope task-055, non corrigé (règle 6). **Sonar (new code)** : `new_violations` 2→0 (2× S2302 « use nameof » dans `ContactController.GetBySourceAsync` fixés — la blacklist ne s'applique pas au new code), `new_bugs`/`new_vulnerabilities` 0, hotspots reviewed 100 %, code_smells projet 0, maintainability A. Le `new_coverage` global (52.9 %) reflète un *new code period* projet très large (~28850 `new_lines`, quasi tout le legacy classé « new ») hors périmètre task-055 ; tout le code task-055 est couvert — accepté best-effort (legacy / Phase 2). **Note environnementale** : suite d'intégration Testcontainers non rejouée dans le run Sonar (couverture du new code assurée par les tests unitaires). **Lint Angular** : build *production* d'apps `mss`/`prescription` échoue sur `environment.prod.ts` manquant — pré-existant, infra de la branche de réécriture, hors task-055. **Control-plane** : `CLAUDE.md` enrichi d'une **règle absolue 12** rendant `ProblemDetails` (RFC 7807) obligatoire pour toute réponse d'erreur de controller et interdisant les nouveaux try/catch boilerplate par action, référençant `GlobalExceptionHandler` comme mécanisme canonique. Aucune règle Ségur déclarée (`**Closes RG**:`) — refactor technique transverse hors couloir Ségur (robustesse/observabilité plateforme). Annexe C enrichie avec task-055. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.30** : polish UX — task-054 (Retrait du champ RPPS de l'opt-in MSSanté) livrée sur `client-blazor` (PR #54, `awaiting-human-merge`) et `client-angular` (mode code-only, uncommitted sur `feature/nova-rewriting-mss`). **Pourquoi** : depuis task-049 (proxy Keycloak), `mssRpps` est extrait server-side du PSC token au moment de l'opt-in et persisté comme attribut utilisateur Keycloak. Le champ formulaire « Numéro RPPS / ADELI » des deux frontends (Blazor `MailSetup.razor` + Angular `MssSetupComponent`) collectait une valeur **jamais envoyée nulle part** : ni `MssOnboardingService.PersistMssEmailAsync` ni son équivalent Angular `persistMssEmail` ne lisaient la valeur, le service POSTait uniquement `{ email }`. Le champ était provablement dead-code et dégradait l'UX en demandant au praticien une saisie redondante avec ce que le serveur sait déjà via la session PSC. **Changements** : (1) **Blazor `MailSetup.razor`** — retrait du bloc `<label>` + `<InputText>` + `<ValidationMessage>` du champ RPPS et de la propriété `Rpps` du modèle interne `SetupModel` (avec ses annotations `[Required]` + `[RegularExpression(@"^\d{9}(\d{2})?$")]`) ; (2) **Blazor `Localizer.cs`** — retrait des clés `MssOnboarding_RppsLabel` et `MssOnboarding_RppsPlaceholder` (FR + EN, 4 lignes, audit grep confirmant aucune autre référence) ; (3) **Blazor `MailSetupPageTests.cs`** — ajout d'une assertion anti-régression `Assert.Throws<ElementNotFoundException>` sur `data-test=mss-setup-rpps-input`, retrait des 3 `.Change("123456789")` qui peuplaient le champ ; (4) **Angular `mss-setup.component.ts`** — retrait du `RPPS_PATTERN` et du contrôle `rpps` du `setupForm` ; (5) **Angular `mss-setup.component.html`** — retrait du bloc `<label>` + `<input>` + erreur conditionnelle RPPS ; (6) **Angular `mss-setup.component.spec.ts`** — simplification du helper `fillForm` (1 paramètre au lieu de 2), assertion anti-régression sur l'absence du `data-testid` RPPS, retrait du `'12'` du test "invalid form". **Cleanup purement subtractif** : aucun changement de comportement, aucun changement de contrat HTTP, le flow `POST /api/v1/account/mss-imap-test` → `PUT /v1/admin/mss-profile` avec `{ email }` est strictement inchangé. **Iso-fonctionnel après merge** : les deux frontends restent alignés (un champ + un bouton + écran de succès). **Tests** : Blazor 86 / 88 verts (2 skipped historiques `BiologyAckPanelComponentTests`) / 0 failed ; Angular `nx affected -t test` → 11 projets verts, `mss-lib` 183 / 183. **Lint Angular** : `nx affected -t lint --projects=tag:scope:mss` baseline → 0 erreur / 0 warning sur les 11 projets affectés, zéro itération consommée. `dtos-mss` : branche `feat/task-054-...` créée proactivement par `/start` (auto-include) mais aucun commit — pas de PR. Aucune règle Ségur déclarée (`**Closes RG**:`) — la task ferme un dead-code UX orthogonal aux exigences REM/Ref#2. Annexe C enrichie avec task-054. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.29** : clôture des prérequis d'enforcement task-048 (cross-check PSC/KC) — **task-049** (opt-in `mssSub`/`mssRpps` côté backend-auth) et **task-050** (Protocol Mappers Keycloak qui projettent ces attributs en claims du JWT KC) livrées **hors automation forge**, directement dans le proxy Keycloak via WindSurf (task-049) et dans le realm Keycloak (task-050). **Pourquoi hors workspace** : task-049 vise un repo `backend-auth` qui n'est pas dans le polyrepo de la forge, et task-050 modifie la configuration du realm Keycloak (côté `devops`, hors automation forge). Les deux tasks sont des prérequis runtime pour que l'enforcement actif côté `api-mail` (`MSS_ENFORCE_PSC_IDENTITY=true` poussé via le commit `65c1655` sur PR #70) produise des 403 cohérents plutôt que des 403 systématiques sur des sessions sans claims. **task-049 (opt-in)** : lors du flow d'opt-in MSSanté existant (`PUT /v1/admin/mss-profile` avec `{email}` envoyé par Angular `mss-onboarding.service.ts::persistMssEmail`), le backend extrait désormais `sub` et `SubjectNameID` du PSC token attaché à la session courante (jamais depuis le payload client — contrat de sécurité non-négociable) et les persiste comme attributs utilisateur `mssSub`/`mssRpps`. Refus strict de re-binding incohérent (409 + log Warning si un user a déjà `(mssRpps, mssSub)` ≠ celui du PSC token courant). Idempotence sur valeurs identiques. Casse stricte sur `SubjectNameID` (capital D final). **task-050 (Protocol Mappers)** : deux mappers `User Attribute` créés sur le scope MSSanté du realm, projetant `mssSub` et `mssRpps` (Claim JSON Type `String`, `Add to access token = true`, `Add to userinfo = true` pour debug). **Audits side-effects** : `client-angular` ✅ aucun changement (le service onboarding posait déjà uniquement `{email}` depuis task-037, l'extraction des claims PSC se fait server-side dans le proxy) ; `api-mail` ✅ aucun changement (la consommation des claims `mssSub`/`mssRpps` est déjà câblée dans `UserContextEnricherMiddleware.ApplyPscKcCrossCheckAsync` via `context.User.FindFirstValue(...)` livrée par task-048). **Pipeline d'enforcement complet** : (1) user se connecte → PSC token reçu par le proxy ; (2) proxy persiste `mssEmail` + `mssSub` + `mssRpps` au moment de l'opt-in (task-049) ; (3) prochaine émission de JWT KC : les mappers projettent les 3 attributs en claims (task-050) ; (4) `api-mail` exécute le cross-check à deux facteurs (task-048) sur chaque requête online ; en mode `Enforce=true` (commit `65c1655` du merge task-048), mismatch ou claim manquant → 403. **Suivi opérationnel** : EventId 3722 (KC token incomplete) en Seq donne le taux de retardataires non-encore-opt-in ; à mesurer pendant 24 h post-déploiement, cible < 5 % du trafic online avant communication utilisateur dédiée. Tasks archivées sans PR (work hors workspace) ; entrées Annexe C tracent l'apport au chantier sécurité.

**Changelog v1.28** : ouverture du chantier **Sécurité — défense en profondeur PSC/KC (cross-check d'identité)**. Task-048 (Cross-check d'identité à deux facteurs entre le JWT Keycloak et le token PSC) livrée sur `api-mail` (PR #70, `awaiting-human-merge`). **Pourquoi** : incident de production confirmé (2026-05-19) où un praticien authentifié via Pro Santé Connect avec sa propre carte CPS (Dr Kit) s'est vu afficher la boîte aux lettres MSSanté d'un autre praticien (Virginie) après un changement de carte CPS sur le même navigateur. Cause racine côté `api-mail` : `UserContextEnricherMiddleware` lisait `X-PSC-Token` sans rapprochement contre l'identité portée par le JWT Keycloak — le token PSC servant ensuite de credential OAuth2 IMAP, le serveur MSSanté ouvrait la mailbox du **propriétaire du token PSC**, pas celle de l'utilisateur authentifié côté KC. **Désormais** : nouveau cross-check à deux facteurs dans `ApplyAuthenticatedUserAsync` — `KC.mssSub == PSC.sub` ET `KC.mssRpps == PSC.SubjectNameID` (Ordinal strict), avec casse exacte critique sur `SubjectNameID` (capital D — confirmé par un payload PSC décodé réel le 2026-05-19) ; nouveau helper `TryParsePscIdentity` qui décode le PSC JWT via `JsonWebTokenHandler.ReadJsonWebToken` **sans validation crypto** (la signature PSC est validée en aval par le serveur MSSanté lors du `SaslMechanismOAuth2` — note 1 du task) ; feature flag `PscIdentityOptions.Enforce` (config section `PscIdentity:Enforce` + override env var `MSS_ENFORCE_PSC_IDENTITY` pour flip runbook sans redeploy) ; **mode observation par défaut** (`Enforce=false`) qui exécute la logique de check mais laisse passer toutes les requêtes — sert à mesurer la prévalence du leak en prod avant la coupure ; **mode enforcement** (`Enforce=true`) retourne 403 avec body JSON `{"error":"Identity check failed."}` (Content-Type `application/json`) dès qu'un claim KC est manquant OU qu'un mismatch est détecté. **KC-only (pas de `X-PSC-Token`, mode offline)** : cross-check skippé — le mode dégradé sans PSC reste fonctionnel. **Onboarding `/api/v1/account/mss-imap-test`** : comportement préservé via l'exclusion existante de `ExcludedPathPrefixes`. **Logging structuré** : 3 nouveaux `LoggerMessage` source-generator — EventId 3721 Information (match nominal, observation seulement), EventId 3722 Warning (KC token incomplete — missing mssEmail/mssSub/mssRpps), EventId 3723 Warning (PSC/KC identity mismatch). Champs anonymisés exclusivement : `MssEmail` via `AnonymiseEmail()` (3 premiers chars + ellipsis + domaine, ex. `doc…@formation.mssante.fr`), `KcSub` / `PscSub` via `TruncateSub()` (6 derniers chars), `KcRpps` / `PscRpps` en clair (RPPS = identifiant national professionnel non sensible), `Path`, `CorrelationId`, `Enforce` (bool). **Jamais de token complet en log**, vérifié par un test dédié (`CrossCheck_Mismatch_LogScope_DoesNotContainFullToken`). **Tests** : 16 nouveaux unit tests (`UserContextEnricherCrossCheckTests` — 4 parsing : token absent / valid / lower-case `SubjectNameId` ignoré / malformed JWT ; 6 observation : matching / missing mssSub / missing mssRpps / mismatch sub / mismatch rpps / no PSC token ; 5 enforcement : matching / missing claims / mismatch sub / mismatch rpps / no PSC = KC-only ; 1 log non-leak) + 5 integration tests HTTP-level via `TestServer` minimal (`PscKcCrossCheckIntegrationTests` — pipeline aligné `UseAuthentication` → `UseAuthorization` → middleware → stub `/api/v1/mail/folders`, avec spy IMAP qui vérifie `IMailClientSessionManager.GetCurrentLockHolder` n'est **pas** appelé sur les requêtes 403) + nouvelle `TestData/PscTokenFixtures.cs` (payload PSC JWT réaliste reproduit depuis un payload décodé réel le 2026-05-19, + helper `BuildKeycloakToken(mssEmail, mssSub?, mssRpps?)`). Les 4 scénarios historiques `UserContextEnricherMiddlewareTests` restent verts (signature `InvokeAsync` étendue avec `IOptions<PscIdentityOptions>`). **Suite api-mail : 2018 / 0 failed** (149 api + 1437 application + 346 infrastructure + 86 domain). **Pre-emptive Sonar cleanup** (commit `c68ce5f`) : consolidation des 3 narrow catch blocks de `TryParsePscIdentity` en un seul `catch (Exception ex) when (ex is ArgumentException or FormatException or InvalidOperationException)` ; extraction des 5 noms de claims JWT (`mssEmail`, `mssSub`, `mssRpps`, `sub`, `SubjectNameID`) en `private const string` pour adresser S1192 (string literal duplicated). **Sonar Phase 1 : 0 issues** sur chacun des 7 fichiers touchés par task-048 (`UserContextEnricherMiddleware.cs`, `PscIdentityOptions.cs`, `DependencyInjection.cs`, `appsettings.json`, `UserContextEnricherCrossCheckTests.cs`, `PscKcCrossCheckIntegrationTests.cs`, `UserContextEnricherMiddlewareTests.cs`, `PscTokenFixtures.cs`). **Limites résiduelles** : (1) pas de vérification cryptographique du token PSC dans cette PR — task-051 ajoutera la vérif JWKS PSC plus tard (defense-in-depth séparée) ; (2) pas d'invalidation `tokenAggregate.pscAccessToken` côté Angular — task-052 la couvrira (defense-in-depth séparée) ; (3) `LogIdentityMatch` est invoqué uniquement quand `!enforce` donc le champ `Enforce` y vaut toujours `false` — conservé pour la symétrie des paramètres entre les 3 events (suggestion non-bloquante notée en review). **Dépendances** : task-049 (opt-in `mssSub`/`mssRpps` côté backend-auth — repo hors workspace) et task-050 (Protocol Mappers Keycloak qui projettent ces attributs en claims du JWT KC — repo `devops` hors automation forge). **Activation production** : `MSS_ENFORCE_PSC_IDENTITY=true` à activer uniquement après task-049 + task-050 déployées et qu'au moins 95 % des sessions KC portent les claims `mssSub`+`mssRpps` (mesuré via taux EventId 3722 en Seq). **Audit grep DOD** : `FindFirstValue("mssSub")` / `FindFirstValue("mssRpps")` dans `UserContextEnricherMiddleware.cs` → ✅ via constantes ; `TryGetPayloadValue<string>("SubjectNameID"` dans le parser → ✅ (capital D strict) ; `"X-PSC-Token"` ailleurs que dans le middleware ou les tests → ✅ rien ne contourne le cross-check. Aucune règle Ségur explicite déclarée (`**Closes RG**:` absent) — la task ferme une faille de sécurité applicative orthogonale aux exigences REM/Ref#2, dont la traçabilité réglementaire passe par le bilan §10 du doc produit. Annexe C enrichie avec task-048. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.27** : passe tech-writer conservatrice. Task-033 (Cleanup Sonar massif api-mail — 20 itérations, 1 PR sans limite de fichiers) livrée sur `api-mail` (PR #50). Réduction mécanique de la dette Sonar : code smells 166 → 124 (-42, -25%), security hotspots 6 → 5 (-1), ratings A/A/A maintenus. 18 règles traitées (CA1861 ×19, CA1822 ×6, xUnit2032 ×5, CA2254 ×4, SYSLIB1045 ×3, xUnit2024 ×3, xUnit2025 ×2, S125 ×3, CA1846 ×2, CA1850 ×1, CA1859 ×2, CA1866/S6610 ×2, S1075 ×1, S6617 ×1, CA2016 ×1, ASP0025 ×1, S1172 ×1, S2139 ×1). 38 fichiers touchés, strictement mécaniques (extraction constantes, méthodes statiques, GeneratedRegex, patterns assertion xUnit, AsSpan, SHA256.HashData, CancellationToken forwarding). S3776 (39) blacklisté, CA1862 (30) skipé (EF Core LINQ risque DB), S1192 (22) skipé (migrations frozen + décisions sémantiques), S107 (9) et S6960 (3) skipés (design decisions). Early-stop après 1 itération — fixes mécaniques safe épuisés. Suite api-mail : 1950 passés / 0 failed. Aucune règle Ségur déclarée — US purement dette technique. Annexe C enrichie avec task-033. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.26** : passe tech-writer conservatrice. Task-032ter (Mock IMAP server — Approche A : extraction interface `IMailClientSessionManager`) livrée sur `api-mail` (PR #49). Extraction de `IMailClientSessionManager` (19 membres) depuis la classe concrète `MailClientSessionManager` — 5 consumers mis à jour (ImapService, ImapFolderService, ImapConnectionService, BackgroundSyncManager, MailExportService), `ImapLockScope` mis à jour, DI remappée `AddSingleton<IMailClientSessionManager, MailClientSessionManager>()`. Retrait de `[ExcludeFromCodeCoverage]` sur 3 services IMAP (ImapService 1914 LOC, ImapFolderService 612 LOC, ImapConnectionService 310 LOC — ~2836 LOC réintégrées au dénominateur de couverture). `BackgroundImapService` conserve l'exclusion (utilise `ImapClient` directement, véritable dépendance IO IMAP — nécessiterait GreenMail/Approach B). Tests existants (57 tests IMAP) mis à jour vers `Substitute.For<IMailClientSessionManager>()` avec délégation `AcquireLockAsync → ImapLockScope.AcquireAsync`. Suite api-mail : 1950 passés / 0 failed. Aucune règle Ségur (`**Closes RG**:`) déclarée — US purement dette technique / outillage qualité, issue du découpage Option B 2026-05-07 de la US d'origine `task-032bis-test-harness` (Chantier 1 — mock IMAP). Annexe C enrichie avec task-032ter. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.25** : passe tech-writer conservatrice. Task-032quater (Samples CDA zip files pour tester CdaParsingService) livrée sur `api-mail` (PR #47). Embarque 5 zips IHE-XDM réels (biologie CR-BIO, prescription cardio, imagerie, microbiologie, DLU) + 1 malformed ZIP dans `tests/mss.mail.integration.tests/Resources/cda-samples/`. 8 tests d'intégration `CdaParsingIntegrationTests` couvrent le happy-path parsing par catégorie CDA, le malformed input, l'option `CdaParseOptions.Metadata`-only, et le chemin inexistant. Retrait de `[ExcludeFromCodeCoverage]` sur `CdaParsingService` (382 LOC réintégrées au dénominateur de couverture). Suite api-mail : 1942 passés / 5 skipped / 0 failed. Aucune règle Ségur (`**Closes RG**:`) déclarée — US purement dette technique / outillage qualité, issue du découpage Option B 2026-05-07 de la US d'origine `task-032bis-test-harness` (Chantier 3 — samples CDA). Annexe C enrichie avec task-032quater. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.22** : durcissement Ségur conformité — task-026 (UI toggle « Bloquer la réponse du patient », SC.MSS/CONF.21 / Référentiel socle MSSanté #2 §3.4.2.3 — ECO.2.2.8) livrée sur `client-blazor` (PR #45) et `client-angular` (mode code-only, uncommitted). **Comble le gap UI découvert pendant la review task-006** : le backend `MssanteHeaderService` (task-001) savait émettre l'en-tête `X-MSS-MES: FIN` quand `MailDto.BlockPatientReply == true` ET au moins un destinataire `*@patient.mssante.fr`, mais aucune surface UI ne permettait au médecin de flipper le booléen — la valeur restait toujours à `false` C# default, donc l'en-tête n'était **jamais émis en pratique** malgré le ✅ Implémenté affiché en §6.13. **Désormais** : checkbox « Bloquer la réponse du patient » dans la toolbar du compose Blazor + Angular, **visible uniquement** quand le predicate `HasPatientRecipient(To, Cc, Bcc)` est vrai (au moins un destinataire `*@patient.mssante.fr` case-insensitive). Reset side-effect : quand le predicate flip back à false (dernière adresse patient retirée), le booléen sous-jacent est forcé à `false` pour empêcher qu'un état caché ne pollue un envoi non-patient suivant. **Convention scellée** : opt-in actif (default false, jamais coché par défaut, jamais auto-coché par heuristique). **Backend** : aucun changement — la logique d'émission est inchangée depuis task-001, et la défense côté serveur (re-vérification du domaine destinataire avant émission) garantit que le toggle UI est une convenience pas un vecteur de sécurité. **DTOs** : aucun changement — `MailDto.BlockPatientReply` et `SaveDraftDto.BlockPatientReply` étaient déjà publiés depuis task-001. **Blazor** : nouveau `BlockPatientReplyHelper` static (47 lignes — `IsPatientAddress` case-insensitive null-safe + `HasPatientRecipient` sur 3 listes) extracté dans `Plugin/Helpers/` pour être unit-testable en isolation sans cérémonie bUnit ; `NewMailComponent.razor` ajoute la checkbox conditionnelle `@if (ShouldShowBlockPatientReplyToggle())` avec `data-testid="compose-block-patient-reply-checkbox"` ; `Localizer` ajoute 2 clés × 2 locales (`BlockPatientReplyLabel`/`BlockPatientReplyTooltip` FR + EN ; tooltip cite Référentiel socle MSSanté #2 §3.4.2.3 — ECO.2.2.8 pour traçabilité réglementaire). **Angular** (code-only, uncommitted sur `feature/nova-rewriting-mss-fixes-20260410`) : modèle `MailDto.blockPatientReply?: boolean` ajouté ; `mail-compose.component.ts` ajoute signal `blockPatientReply` + computed reactif `hasPatientRecipient` (scrute les 3 signals d'adresses) + effect de reset qui track les 3 signals + propagation au payload via `Partial<MailDto>` au moment du `sendMailDirect()` + reset dans `reset()` à la fermeture ; `mail-compose.component.html` ajoute la checkbox conditionnelle `@if (hasPatientRecipient())` avec `data-testid` + tooltip FR inline. Tests : Blazor `HealthPlatform.Module.Mss.Plugin.Tests` 37/37 (était 21, **+16 nouveaux** `BlockPatientReplyHelperTests` Theory/Fact couvrant case-insensitive, null/empty, bare suffix, missing leading @, partial match négatif, in To/Cc/Bcc, uppercase) ; mss-lib Vitest 98/98 (no regression). Sonar **skip** (api-mail non touché). **Audit grep DOD** : `BlockPatientReply` dans Blazor sources → ✅ 3 fichiers (Helper, NewMailComponent, Localizer FR + EN) ; `blockPatientReply` dans Angular sources → ✅ 3 fichiers (model.ts, .ts, .html). **Limites différées** : Vitest spec.ts pour `mail-compose` non créé (le composant n'a pas de spec.ts pré-existant ; créer un greenfield avec mocking ~10 deps dépasse le périmètre task-026 limitée à câbler le toggle) — gap documenté, même posture qu'au merge task-006. **Dans le tableau §6.13 ligne RG-E009-016** : la note ✅ Implémenté est complétée — l'en-tête `X-MSS-MES: FIN` est désormais **opérationnellement émis** quand le médecin coche la case sur un envoi MES (avant task-026 le toggle n'existait pas, l'en-tête n'était jamais émis). Annexe C enrichie avec task-026. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.21** : feature métier — task-006 (« Annule et remplace », AMBU.MSS/va1.02) livrée sur `dtos-mss` (PR #16, NuGet 247.0.0), `api-mail` (PR #42), `client-blazor` (PR #44) et `client-angular` (mode code-only — uncommitted, humain gère commit/push TFS). Permet au professionnel de re-publier une version corrigée d'un document médical déjà envoyé : nouveau message pré-rempli avec subject `[Annule et remplace] {original}` (préserve le marker IHE_XDM `XDM/1.0/DDM+` per ECO.2.1.3 — le préfixe `[Annule et remplace]` est inséré APRÈS le marker XDM pour rester structuré côté destinataire), threading RFC 5322 (`In-Reply-To` = MessageId original, `References` = chaîne existante + MessageId dédupliquée), corps mention `Ce message annule et remplace le message du {date} ayant pour objet : « {sujet sans préfixe XDM} »`, recipients copiés (To + Cc — Bcc volontairement non propagé pour préserver la sémantique blind). **Backend** : nouvelle colonne `Mails.IsCancelled boolean NOT NULL DEFAULT false` (migration consolidée), entité `Mail.IsCancelled`, DTO `MailDto.IsCancelled` (server-managed), `IMailRepository` enrichi de `GetMailByIdAsync(Guid)` + `ResolveMailIdAsync(folder, uid)` + `MarkAsCancelledAsync(Guid)` (idempotent). Nouveau `IMailCancellationService` (Application) avec 2 méthodes principales : `PrepareCancelAndReplaceDraftAsync` charge l'original et bâtit le `MailDto` pré-rempli ; `SendCancelAndReplaceAsync` ré-écrase défensivement les threading headers depuis l'original chargé serveur-side (forge-resistant — un payload forgé par le client ne peut pas court-circuiter la convention), invoque le sendCallback, et marque l'original cancelled atomiquement sur succès (saga : send fail → no mark, send OK + mark fail → 200 + warning). 2 nouveaux endpoints `MailController` keyés sur (folder, uid) pour cohérence avec le reste : `GET /api/v1/mail/folders/{folder}/emails/{uid}/cancel-and-replace-draft` et `POST .../cancel-and-replace`. Mode offline : queue le replacement comme PendingAction `SendMail`. **Blazor** : strike-through subject + badge orange `[ANNULÉ]` sur `MailHeader` (liste inbox, 2 sites de rendu) et sur le détail. Bouton « Annuler et remplacer » dans la toolbar `MailDetailComponent` avec predicate `CanCancelAndReplace` (visible uniquement si `mail.IsCancelled == false && mail.HasMedicalDocuments == true && mail.FolderPath contains "Sent"`). `IMailService` étendu de 2 méthodes `GetCancelAndReplaceDraftAsync(folder, uid)` + `SendCancelAndReplaceAsync(folder, uid, replacement)`. `NewMailComponent` reçoit 3 nouveaux paramètres et `InitializeCancelAndReplace()` pré-remplit le formulaire ; le bouton Send route vers le nouvel endpoint via ternaire si en mode cancel-replace. `ComposeRequestArgs` étendu (CancelAndReplaceOriginalFolder/Uid/Draft). 4 clés Localizer FR + EN (`CancelAndReplace`, `MailCancelledBadge`, `CancelAndReplaceSuccess`, `CancelAndReplaceSuccessDetail`). Fallback gracieux à un Reply si l'original est introuvable serveur-side (deleted/moved entre display et click). **Angular** (code-only, uncommitted) : modèle `MailDto.isCancelled?` ajouté, service `MssApiService` enrichi des 2 méthodes, `mail-detail.component.html` + `.ts` ajoutent le bouton + handler avec Reply fallback, `mail-header.component.html` + `.scss` ajoutent strike-through + badge sur les 2 sites de rendu, `mail-event.service.ts` ajoute le sidecar signal `cancelAndReplaceContext`, `mail-compose.component.ts` route le Send via le nouvel endpoint quand le sidecar est set + clear le sidecar à `doClose()`. Tests : api-mail **1733 / 5 ignorés / 0 failed** (était 1715, +18 nouveaux task-006 — 15 unit `MailCancellationServiceTests` couvrant subject prefix XDM/non-XDM, body mention, threading per RFC, recipients copy, idempotency, mark-saga, defensive header enforcement + 3 integration Postgres-backed `MailRepositoryIntegrationTests` round-trip IsCancelled column / 404 / idempotent mark) ; blazor solution build clean ; mss-lib Vitest 98/98 (existing tests, no regression). Sonar Mode A best-effort accept (3/4 hard targets toujours atteints, diff suit les patterns existants — 0 nouveau CA1873 / S107 / S3776). **Closes RG** : non explicite côté table section 6 — la US référence AMBU.MSS/va1.02 + ECO.2.1.1/2.1.3/2.2.1/2.2.2 (ces 4 ECO sont déjà couverts globalement par d'autres tasks ; AMBU.MSS/va1.02 reste à formaliser dans la table section 6 si la PO le souhaite). **Limites différées** : (1) audit trace `MailCancelAndReplace` non émis pour cohérence avec framework task-004 — follow-up suggéré ; (2) bUnit + Vitest tests pour button visibility et badge rendering différés — gap documenté, chore qualité ; (3) end-to-end Playwright `/qa` pour le flow complet — follow-up. **Note environnementale** : suite api-mail.sln complète bloquée par file lock du dev AppHost (PID 27300), mais aucun fichier de `src/AppHost/` n'est touché — toutes les couches modifiées builds + tests verts. Annexe C enrichie avec task-006. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.20** : passe d'observabilité orthogonale au chantier sécurité — task-024 (instrumentation lock IMAP + fix log-level race AddNewMail) livrée sur `api-mail` (PR #41). Deux patterns d'erreur identifiés en Seq sur la session `virginie.medecinrpps0062267` du 2026-05-03 traités : (1) `MailRepository.AddNewMail` loguait en `Error` un flow de **succès** quand le catch `PostgresErrorCodes.UniqueViolation` rebasculait vers `UpdateExistingMailWithContentAsync` (la race condition concurrent-enrichment était déjà gérée correctement, mais le log level pollue Seq) — corrigé par `LogError → LogInformation`, message reformulé `[DB] Mail UID={Uid} already existed — content updated via duplicate-fallback (MailId={MailId})` ; (2) `MailClientSessionManager.LockImapClientAsync` cancellait des opérations user-driven (`UpdateReadStatus` mark-read, ~1135 ms avant cancel) sans révéler **qui** détenait le lock IMAP au moment du timeout — instrumenté par un slot `(operation, acquiredAt)` thread-safe sur `MailClientSession` (lock minimal `_holderLock`, writer-only update par le thread qui vient d'acquérir le sémaphore, lecture best-effort par les waiters), `GetCurrentLockHolder` exposé sur `MailClientSessionManager`, `ImapLockScope.AcquireAsync` enrichit les catch `OperationCanceledException` (Warning) et `TimeoutException` (Error) avec `HolderOperation` + `HolderHeldMs`, success log `[ImapLock] ✅ Lock acquired` bumpé `Debug → Information` pour rendre la distribution `WaitTimeMs` requêtable en Seq sans activer Debug. **Aucun changement de comportement** : timeout du lock IMAP toujours 120s, pas de nouveau fallback path. Tests : api-mail 1166 application (5 nouveaux tests holder/Information/cancel-warning) + 273 infrastructure + 86 domain + 16 integration repository (2 nouveaux tests Postgres-backed Pattern 1 — `AddNewMailDuplicateFallbackShouldLogInformationNotError` + `AddNewMailDuplicateWithoutContentShouldStillReturnEmptyAndLogDebug`). Sonar Mode A best-effort accept (3/4 hard targets toujours atteints, diff strictement additif via templates de logging structuré, 0 nouveau CA1873 / S107 / S3776). **Note environnementale** : suite `mss.mail.api.tests` non rejouée pendant `/review` à cause du file lock du dev API en cours d'exécution (PID 66188 + Visual Studio 44916), aucun fichier `src/Api/` n'est touché par cette PR — impact fonctionnel nul. Cette task **prépare task-025** : la phase 1 d'analyse de task-025 attend ~5 jours ouvrés de logs `Lock IMAP acquired by ... after ...ms` + `Cancelled while waiting ... HolderOperation=...` collectés par cette instrumentation pour décider de la stratégie fallback (PendingActions queue / timeout bump / pool IMAP split / combinaison). Annexe C enrichie avec task-024. Sections 4/5/6 hand-crafted préservées. Le bilan sécurité §10.7 reste inchangé (task-024 est observability, pas une couche défensive supplémentaire).

**Changelog v1.19** : **clôture définitive du chantier Sécurité E009** — couche 3 (ownership scoping repositories, task-023) livrée sur `api-mail` (PR #40). Ferme le **dernier vecteur IDOR** identifié dans l'audit task-020 : avec task-021 (JWT crypto) et task-022 (SSE secured), un attaquant ne peut plus se faire passer pour autrui par header / query string ; mais si un Guid de ressource leak (logs Seq, screenshot, audit trail partagé, breach), un user authentifié légitime peut encore accéder à la ressource d'un autre tenant car les repositories ne filtrent pas par `UserId`. **Désormais** : ajout colonne `UserId Guid NOT NULL + FK Users(Id) + IX_*_UserId` sur les 4 tables qui en manquent (`Contacts`, `ContactGroups`, `MailTemplates`, `PendingActions`) dans la migration consolidée `20240101_SetupMigration.cs` ; 4 entités domaine portent `UserId` ; `BaseRepository.GetCurrentUserIdAsync` factorise les 3 helpers `GetOrCreateUserIdAsync` dupliqués (single source of truth, race 23505 catch + ChangeTracker.Clear + re-read pattern) ; **5 dépôts** scopés cumulativement par UserId : `ContactRepository` (9 méthodes + filtre proactif `FilterOwnedGroupIdsAsync` anti join-table tampering sur `Create`/`Update`), `MailSignatureRepository` (`GetByIdAsync`/`DeleteAsync`), `MailTemplateRepository` (5 méthodes, `UpdateAsync` re-load pour vérifier ownership avant mutation, `CreateAsync` setter UserId), `AuditTraceRepository` (`GetTracesAsync`/`GetByIdAsync` filtrent par `UserId == UserContextInfo.Email` — string par design task-020 pour préserver la traçabilité même si le User row est supprimé), `PendingActionRepository` (9 méthodes, `AddAsync` setter UserId automatique). **Convention scellée** : (a) toute query par Id filtre cumulativement par UserId, (b) toute table métier per-user porte `UserId Guid NOT NULL + FK + index`, (c) controllers retournent **404 sur ownership KO, jamais 403** pour ne pas leaker l'existence des Guids, (d) `BaseRepository.GetCurrentUserIdAsync` est l'unique voie d'accès au UserId courant côté repo. Tests : api-mail **1708 passés / 0 failed** (était 1587 — **+21 tests cross-tenant** dans `CrossTenantOwnershipTests.cs` couvrant Contact, MailSignature, MailTemplate, MssAuditTrace, PendingAction ≥ 3 tests par dépôt ; existants adaptés via seed User row + tag UserId sur entités directement insérées) ; client-blazor / mss-lib non touchés (US purement back-end). **Audit grep DOD** : `FindAsync([id])` repos métier → ✅ vide ; `FirstOrDefaultAsync(...Id == id)$` sans `&& UserId ==` → ✅ vide. Sonar Mode A best-effort accept sans cleanup (3/4 hard targets déjà atteints baseline 728 code smells dont 32 S3776 blacklisté + 387 CA1873 logging — campagne dédiée future ; aucune nouvelle violation introduite par task-023, le diff suit le pattern message-template existant). **Dans le tableau §10.7 défense en profondeur** : couche 3 passe de 🔴 À faire à 🟢 Implémentée — **les 3 couches du chantier sécurité E009 sont désormais en place**. **Bilan E009 sécurité (6 tasks 018+019+020+021+022+023)** : (1) 100% des PK Postgres en `uuid` v7 (anti-énumération), (2) JWT validation crypto + secure-by-default `FallbackPolicy` (anti-spoofing), (3) SSE / endpoints anonymes refermés (anti-leak temps réel), (4) ownership scoping cumulatif sur les repos exposant des Guid (anti-cross-tenant). Le seul vecteur IDOR restant identifié serait un compromis de la base User elle-même — hors périmètre application. Annexe C enrichie avec task-023. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.18** : couche 2bis du chantier Sécurité — SSE & endpoints anonymes (task-022) livrée sur `api-mail` (PR #39), `client-blazor` (PR #43) et `client-angular` (code-only, uncommitted). Ferme la faille IDOR temps réel identifiée dans l'audit : un attaquant avec son propre JWT valide pouvait s'abonner au flux SSE d'une autre victime via `?email=victim@x.fr`. **Désormais** : retrait du `[AllowAnonymous]` temporaire posé en task-021 sur les 5 controllers (`AiController`, `DirectoryController`, `FeatureFlagController`, `MailEventsController`, `NotificationsController`) — désormais protégés par la `FallbackPolicy = RequireAuthenticatedUser` ; `MailEventsController.Stream` et `NotificationsController.Stream` refactorés pour résoudre l'email **exclusivement** depuis le claim JWT validé (`User.FindFirstValue(ClaimTypes.Email)` avec fallback sur `JwtRegisteredClaimNames.Email` puis `preferred_username`), le paramètre `?email=` query string est désormais ignoré (un attaquant qui passe `?email=victim@x.fr` avec son propre JWT valide voit s'ouvrir un flux sur SON propre email, jamais celui de la victime) — 400 BadRequest si claim email manquant. Nouveau scrubber `RequestLoggingMiddleware.ScrubQueryStringToken` (regex partial generated, IgnoreCase) qui masque `?token=...` / `&token=...` → `token=***` AVANT d'enrichir LogContext — le JWT propagé en query string pour les flux SSE n'apparaît jamais en clair dans les logs Seq. Frontends adaptés : Blazor `MailSseService.BuildStreamUrl()` drop `email=` du query, garde `folder` + `token` ; Angular `notification-stream.service.ts` drop `email=`, garde `token=`. Tests : api-mail 1587 passés / 5 skipped / 0 failed (était 1577 — **10 nouveaux tests sécurité** : 8 theory `ScrubQueryStringTokenMasksTokenValueButPreservesOtherParams` + 1 multi-occurrence + 1 anti-spoofing `StreamIgnoresEmailQueryStringAndUsesClaimInstead` + 1 missing-claim `StreamWithoutEmailClaimReturns400`) ; client-blazor 21/21 ; mss-lib Vitest 98/98. Audit grep DOD : `[AllowAnonymous]` dans Controllers → ✅ vide, `Headers["Client-Email"]` lu → ✅ vide, `?email=` dans MailEventsController + NotificationsController → ✅ vide (le seul `[FromQuery] string email` restant est `SettingsController.GetAutoconfigAsync`, paramètre métier autoconfig DNS, pas un identifiant d'auth — légitime). Sonar Mode A best-effort accept sans re-analyse (3/4 hard targets déjà atteints, task-022 net LOC négatif -83 lignes + 10 nouveaux tests sécurité, aucun nouveau cluster d'issues attendu, coût d'analyse complète disproportionné vs ROI). **Dans le tableau §10.7 défense en profondeur** : couche 2bis passe de 🔴 À faire à 🟢 Implémentée. **Suggestions non-bloquantes** notées : durcir CORS policy en prod (whitelist explicite des origins, dev reste `AllowAnyOrigin` pour `/qa` et hot-reload) ; cleanup mineur de `MailSseService._currentUserEmail` (résiduel logging) ; tests HTTP-pipeline `WebApplicationFactory<Program>` toujours absents — carry-over task-021, couvert opérationnellement par `/qa`. Annexe C enrichie avec task-022. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.17** : couche 2 du chantier Sécurité — Authentification cryptographique JWT (task-021) livrée sur `api-mail` (PR #38). Ferme la faille de spoofing d'identité par header `Client-Email` identifiée dans l'audit IDOR : auparavant `RequestHelper.TryExtractJwtToken` lisait les headers `Authorization`, `Client-Email`, `Client-Session-Id` sans aucune validation crypto — un attaquant qui devine un email pouvait forger une requête `Client-Email: victim@x.fr` + `Authorization: Bearer DEADBEEF` et le serveur traitait la requête comme provenant de la victime. **Désormais** : `AddJwtBearer` Keycloak (signature + issuer + audience + lifetime) ; `PolicyScheme JwtOrTestBypass` qui dispatche entre `TestBypassAuthenticationHandler` (auth scheme dédié pour `X-Test-Bypass`, hard-block en Production — utilisé par `/qa`) et `JwtBearer` ; `FallbackPolicy = RequireAuthenticatedUser` secure-by-default (toute route est `[Authorize]` implicitement, infra publique `[AllowAnonymous]` explicite) ; nouveau `UserContextEnricherMiddleware` qui peuple `UserContextInfo` depuis les claims JWT validés (Email = claim `email` ou `preferred_username`, ClientSessionId = `sid` ou `jti`, KeycloakToken via `SaveToken = true`) et lit `X-PSC-Token` du header inconditionnellement (drives `IsOnlineMode`, indicateur de session PSC pas un secret d'auth) ; **suppression de ~120 appels** `RequestHelper.TryExtractJwtToken` dans 23 controllers (helper marqué `internal`, `InternalsVisibleTo("mss.mail.api.tests")` câblé pour préserver les tests existants) ; mode **dev permissif** quand `Keycloak:Authority` absent ET `ASPNETCORE_ENVIRONMENT != Production` — JWT signature non validée pour permettre le dev local sans Keycloak réel (Production force toujours la validation complète) ; token accepté en query string `?token=...` pour les flux SSE (`EventSource` natif Angular ne sait pas poser de header) via `OnMessageReceived` JwtBearerEvents ; 5 controllers actuellement anonymes (`AiController`, `DirectoryController`, `FeatureFlagController`, `MailEventsController`, `NotificationsController`) marqués `[AllowAnonymous]` temporairement — task-022 décidera lesquels rouvrir. Tests : 1577 passés / 5 skipped / 0 failed (était 1587, 10 tests obsolètes "Unauthorized when missing headers" supprimés par design — l'assertion a perdu son sens, l'auth est dans le pipeline AuthN/AuthZ pas dans le handler). Audit grep DOD : `TryExtractJwtToken` dans Controllers → ✅ vide, `[AllowAnonymous]` → 5 entrées (base task-022). **Pass 6 (tests sécurité HTTP-pipeline dédiés via `WebApplicationFactory<Program>`)** deferred, couvert opérationnellement par `/qa` Playwright en test-bypass mode. **Hotfix utilisateur** : un bug "considéré hors ligne malgré PSC connecté" identifié pendant le test manuel Angular a été corrigé en passe successive (commit `9dc7eb4`) — le middleware lisait `X-PSC-Token` uniquement à l'intérieur du bloc `if (IsAuthenticated)`, désormais lecture inconditionnelle ; le mode dev permissif a aussi été ajouté au même moment pour permettre à l'app Angular d'authentifier en local sans Keycloak réel. Dans le tableau §10.7 défense en profondeur : couche 2 passe de 🔴 À faire à 🟢 Implémentée. Annexe C enrichie avec task-021. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.16** : phase 3 et **clôture** du chapitre Sécurité — Identifiants opaques Guid v7. Task-020 (User / Contact / Audit cluster + scellement final de la convention) livrée sur `dtos-mss` (PR #15), `api-mail` (PR #37), `client-blazor` (PR #42) et `client-angular` (mode code-only — uncommitted, humain gère commit/push TFS et PR). Migre les 10 entités restantes vers Guid v7 PK + FK : `User`, `UserSetting`, `MailSignature`, `MailTemplate`, `MailFolder`, `Contact`, `ContactMssAddress`, `ContactTag`, `ContactGroup`, `ContactGroupMember`, `MssAuditTrace`, `PendingAction` ; et finalise `MailMedicalDocument.PractitionerContactId` resté `int?` en task-018 par cohérence FK avec `Contact.Id` désormais Guid. **Suppression du hack `ContactDto.GetIntId()`** (BitConverter sur 4 bytes du Guid, ~1/65k birthday-collision) — l'item le plus impactant en sécurité du chantier complet ; les consumers passent désormais `ContactDto.Id` directement puisque `Contact.Id` est Guid à la source. **MailDataContext** : 8 nouveaux `UuidV7ValueGenerator` câblages (`User`, `UserSetting`, `MailSignature`, `MailTemplate`, `MailFolder`, `MssAuditTrace`, `PendingAction`, `Tag`) ; **0 `UseIdentityAlwaysColumn` restant** dans le DataContext (audit grep complet). **Migration consolidée** `20240101_SetupMigration.cs` : 12 tables `AsInt32().PrimaryKey().Identity()` → `AsGuid().PrimaryKey()`, propagation Guid sur les FK `UserId` / `ContactId` / `GroupId` / `PractitionerContactId` ; colonnes int légitimes préservées (`Uid` IMAP, `SortOrder`, `UrgencyLevel`, `FolderType`, `TotalCount`/`UnreadCount` compteurs, `MailUid` IMAP, `RetryCount`, `Status` enum). **Routes API** : `ContactController` 8 routes `{id:int}`/`{contactId:int}`/`{groupId:int}` → `:guid`, `MailTemplateController` 3 routes, `SignatureController` 5 routes, `AuditController` 1 route, `MailController.CancelPendingEmail` `{id}` → `{id:guid}` — soit **17 routes migrées** ; en sortie de cette task **0 `{*:int}` ne subsiste** dans aucun controller. **DTOs** : `MailSignatureDto`, `MailTemplateDto`, `MssAuditTraceDto` Id Guid (`UserId` reste string — identifiant JWT), `MailMedicalDocumentDto.PractitionerContactId` Guid?, `ManagementDtos` 6 records. NuGet `HealthPlatform.Dtos.Mss 239.0.0` publié via CI auto-versioning ; consumers (`api-mail` + `client-blazor`) bumpés via `Directory.Packages.props`. **Repositories / interfaces / services** propagent Guid. **ContactRepository** : suppression du hack `BitConverter.ToInt32(group.Id.ToByteArray(), 0)` dans `UpdateGroupAsync` et de l'appel à `contact.GetIntId()` dans `UpdateAsync`. **Blazor** : signatures Guid sur 3 services, composants adaptés (locaux `_selected*Id` int? → Guid?, pattern-match `value is int` → `value is Guid`). **Angular** (mode code-only) : 11 fichiers TS adaptés. **Tests** : nouveau helper `TestGuid.From(int)`. Suite api-mail **1587 passés / 5 skipped / 0 failed** ; client-blazor **21/21** ; mss-lib Vitest **98/98**. **Scellement final (audit grep complet)** : `int Id { ` dans `Domain/Entities/` → ✅ vide ; `{id:int}|{contactId:int}|...` dans `Api/Controllers/` → ✅ vide ; `.AsInt32().PrimaryKey()` dans `Migrations/` → ✅ vide ; `public int Id { ` dans `Dtos/` → ✅ vide ; `GetIntId` source code → ✅ vide ; `id: number` dans `Client/Angular/.../models/` → ✅ vide hors `uid`/`mailUid`/`emailUid` IMAP. Annexe C enrichie avec task-020. Section **Sécurité** mise à jour avec le bilan final. Sections 4/5/6 hand-crafted préservées.

**Changelog v1.15** : phase 2 du chapitre Sécurité — Identifiants opaques Guid v7. Task-019 (Migration cluster Mail + finalisation `MailMedicalDocument.MailId` vers Guid v7) livrée sur `dtos-mss` (PR #14), `api-mail` (PR #36), `client-blazor` (PR #41) et `client-angular` (mode code-only — uncommitted). Migre `Mail`, `MailContent`, `MailRecipient`, `MailAttachment`, `Tag`, `MailTag` (6 entités) vers Guid v7 PK + FK, et finalise `MailMedicalDocument.MailId` resté int en task-018 par cohérence FK. Réutilise le `UuidV7ValueGenerator` introduit en task-018. Migration consolidée `20240101_SetupMigration.cs` éditée : 6 tables `AsGuid().PrimaryKey()` + propagation Guid sur les FK (`MailId`, `TagId`). **Pas de routes API à migrer** car `MailController` utilise les UID IMAP cross-boundary (string `{emailid}`), pas la PK Postgres `Mail.Id`. **DTOs** : 4 fichiers cluster Mail migrés. NuGet `HealthPlatform.Dtos.Mss 235.0.0` publié via run 25212643824 ; consumers bumpés. **Repositories / messages / internal models** propagent Guid : `IMailRepository.AddNewMail` retourne `Task<Guid>` (était `Task<int>`) ; helpers `MailRepository` `Dictionary<int,…>` → `Dictionary<Guid,…>` (5 helpers) ; modèles internes `EmailContentWithEmbedding` (Id+MailId), `MedicalDocumentWithEmbedding.MailId`, `FullTextSearchResult.MailId` Guid ; `AddNewMailMessage.MailId` Guid. Mock repository retourne `Guid.Empty` au lieu de `0`. **Blazor** : impact UI minimal (MailDto utilise Uid IMAP cross-boundary, TagDto.Id opaque). **Angular** (code-only) : 1 fichier modifié (`mail.model.ts`). Tests : api-mail 1687 / 21 ignorés / 0 échec (TestDataFactory + 7 fichiers domain entity tests + 6 fichiers services/consumers/repository fixtures Guid) ; client-blazor 21/21 ; mss-lib Vitest 98/98. Sonar pré-PR : 3/4 hard targets atteints, baseline 728 inchangé. Annexe C enrichie avec task-019.

**Changelog v1.14** : ouverture du chapitre **Sécurité — Identifiants opaques (Guid v7)**. Task-018 (Migration des PK de cluster Patient + MailMedicalDocument vers UUID v7 — RFC 9562) livrée sur `dtos-mss` (PR #13), `api-mail` (PR #35), `client-blazor` (PR #40) et `client-angular` (mode code-only — uncommitted). **Pourquoi** : durcissement sécurité — élimine l'énumération IDOR sur les routes `/medical-documents/{int}` et leurs équivalents. UUID v7 plutôt que v4 pour le tri temporel + B-tree friendliness sur grosses tables. **Stratégie « à la source »** : pas de cohabitation `int Id` interne + `Guid PublicId` externe — les colonnes `Id` Postgres elles-mêmes deviennent `uuid`. Génération côté .NET via un nouveau `UuidV7ValueGenerator` (`Guid.CreateVersion7()`, .NET 10 natif) câblé dans `MailDataContext` via `HasValueGenerator<>().ValueGeneratedOnAdd()` sur les 4 entités du cluster (`MailPatient`, `MailMedicalDocument`, `MailMedicalDocumentBiology`, `MailMedicalDocumentSummary`) — colonne Postgres `uuid` sans default DB. **Migration** : édition directe de la migration consolidée `20240101_SetupMigration.cs` — `AsInt32().PrimaryKey().Identity()` → `AsGuid().PrimaryKey()` sur les 4 tables, propagation Guid sur les FK `PatientId` / `DuplicateOfId` / `MedicalDocumentId`. `MailId` reste `int` (sera migré task-019), `PractitionerContactId` reste `int` (task-020). Pre-flight chore commit `52c0994` consolide les 10 migrations historiques standalones. **Routes API** : `MedicalDocumentsController` 3 routes `{documentId:int}` → `{documentId:guid}` + validation `request.PatientId == Guid.Empty`. **DTOs** : 13 fichiers cluster migrés. NuGet `HealthPlatform.Dtos.Mss 231.0.0` publié via GH Actions run 25176890465. **Repositories / services / messages** propagent Guid. **Blazor** : `IPatientService` 3 signatures Guid + composants adaptés. **Angular** (mode code-only) : 11 fichiers TS adaptés. Tests : api-mail 1687 / 21 ignorés / 0 échec ; client-blazor 21 / 21 bUnit ; mss-lib Vitest 98 / 98. Sonar pré-PR : 3/4 hard targets atteints. **Décision arch sauvée en mémoire forge** (`project_id_strategy_guid_v7.md`). **Découpage en 3 tasks** : task-018, task-019, task-020. Annexe C enrichie avec task-018. Section **Sécurité** ajoutée.

**Changelog v1.13** : passe tech-writer conservatrice. Task-013 (Détection des doublons CDA — SC.CDA/INT.18) livrée sur `dtos-mss` (PR #12), `api-mail` (PR #34), `client-blazor` (PR #39) et `client-angular` (mode code-only — uncommitted). Deux nouveaux champs `IsDuplicate` (bool) + `DuplicateOfId` (int?) sur `MailMedicalDocumentDto` et un nouveau record `DuplicateDecisionRequestDto`. À la réception, `MailRepository.FindExistingDuplicateOfAsync` flagge tout CDA partageant le même `DocumentId` OU la combinaison fonctionnelle `Ins + Category + Date + Title` avec un document existant. Migration FluentMigrator `20260429120000` ajoute les deux colonnes + self-FK `FK_MailMedicalDocuments_DuplicateOf` (`OnDelete SetNull`) + index `IX_MailMedicalDocuments_DuplicateOfId`. Nouvel endpoint `POST /api/v1/medical-documents/{id}/duplicate-decision`. **Blazor** : badge orange « DOUBLON » sur la ligne d'inbox, bannière dans `MailDetailComponent` avec chip statique « Original : doc #N » + boutons Confirm/Reject. **Angular** (mode code-only) : badge `mail-duplicate-badge` sur `mail-header.component`, bannière `mail-detail-duplicate-banner` sur `mail-detail.component`. 12 clés FR + EN. Tests : 4 in-memory `MailRepositoryTests` + 3 `PatientRepositoryTests` + 3 `PatientServiceTests` + 1 round-trip Postgres `PatientRepositoryIntegrationTests` + 3 bUnit Blazor + 3 Vitest Angular `mail-header.component.spec.ts`. Suite api-mail 1677 passés / 21 skipped / 0 failed. Sonar pré-PR : 7 fixes `external_roslyn:ASP0015`. Annexe C enrichie avec task-013.

**Changelog v1.12** : finalisation task-012 après tests humains. Le PO a retiré explicitement le bouton « Créer un nouveau patient » de la dialog `PatientAttachmentDialog` côté Blazor ET côté Angular. Localizer keys `AttachmentDialog_CreateNewPatient` + `AttachmentDialog_CreateNewPatientNotImplemented` retirées (FR + EN). Décision sauvée en mémoire forge (`feedback_attachment_workflow_no_create_new_patient.md`). **UI Angular livrée par la forge (mode code-only)** : nouveau `PatientAttachmentDialogComponent` (standalone + OnPush + signals + JSDoc + 11 tests Vitest) et bannière intégrée à `MailDetailComponent` Angular. Tests mss-lib : 95 passés / 0 failed. Code Angular uncommitted sur branche `feature/nova-rewriting-mss-fixes-20260410`. Polish visuel Blazor : extraction des inline styles bannière vers `MailDetailComponent.razor.css`, `PatientAttachmentDialog.razor.css` retravaillé pour le thème dark. Annexe C task-012 mise à jour. PRs api-mail #33, client-blazor #38, dtos-mss #11.

**Changelog v1.11** : passe tech-writer conservatrice. Task-012 (Rattachement patient par comparaison visuelle — workflow LGC.MDV de complément à task-011) livrée sur `dtos-mss` (PR #11), `api-mail` (PR #33), `client-blazor` (PR #38) et `client-angular` (mode code-only — uncommitted). Deux nouveaux DTOs : `PatientMatchCandidateDto` et `AttachPatientRequestDto`. Deux nouveaux endpoints sur `api-mail` : `GET /api/v1/patients/match?...` (jusqu'à 10 candidats classés par score pondéré) ; `POST /api/v1/medical-documents/{id}/attach-patient`. **Blazor** : nouvelle dialog `PatientAttachmentDialog` + bannière amber injectée dans `MailDetailComponent` ; mise à jour locale optimiste de `PendingIntegrationsCount` au succès. 22 clés de localisation FR + EN. Tests : 4 service unit + 1 repo unit + 4 repo integration sur Postgres réel (Testcontainers) ; 18 tests bUnit Blazor ; suite api-mail 1666 passés / 21 skipped / 0 failed. **Sonar bundled** : itération 1 best-effort résolvant 5 occurrences de `csharpsquid:S1192`. Annexe C enrichie avec task-012.

**Changelog v1.10** : passe tech-writer conservatrice. Task-011 (Indicateur visuel d'intégration des documents médicaux — LGC.MDV.06) livrée sur `dtos-mss` (PR #10), `api-mail` (PR #32), `client-blazor` (PR #37) et `client-angular` (mode code-only — uncommitted). Le DTO `MailMedicalDocumentDto` expose désormais `PatientId`, et `MailDto` reçoit un agrégat `PendingIntegrationsCount` calculé côté serveur. Frontends Blazor (`MailHeader.razor` + `MailBodyComponent.razor` tabs) et Angular (`mail-header.component` + `mail-body.component` tabs) rendent le badge vert (✓ "tous intégrés") ou orange (⏳ avec compteur "N en attente"). 9 nouveaux tests (3 xUnit `MailRepositoryTests` + 3 bUnit `MailHeaderIntegrationIndicatorTests` + 3 vitest `mail-header.component.spec.ts`). Coverage `api-mail` : 49.6 % → 50.2 % (+0.6 pt). Annexe C enrichie avec task-011.

**Changelog v1.9** : passe tech-writer conservatrice. Task-014 (Options sélectives de parsing CDA — `CdaParseOptions [Flags]`) livrée sur `api-mail` (PR #31). Le service `CdaParsingService.ParseIheXdmZip` accepte désormais un paramètre optionnel `CdaParseOptions options = All`. `MssanteHeaderService` (livré par task-001) bascule sur `CdaParseOptions.Metadata` pour ne lire que `CdaTypeCode` + qualification INS sans déclencher la transformation HTML XSLT à chaque envoi de mail. Élimine le bruit log `[CdaParsingService] HTML transform error: XSLT compile error.` Annexe C enrichie avec task-014.

**Changelog v1.8** : passe tech-writer conservatrice. Task-001 (En-têtes SMTP MSSanté `X-MSS-CODECDA`, `X-MSS-INS`, `X-MSS-NIL`, `X-MSS-MES`) livrée sur `api-mail` (PR #30) et `dtos-mss` (PR #9). Nouveau `MssanteHeaderService` câblé dans `SmtpService.SetMessageHeaders` ; décode l'archive `ihe_xdm.zip` côté serveur. Closes RG-E009-009, RG-E009-010, RG-E009-011, RG-E009-016, RG-E009-087. **Mises en garde opérationnelles** : (1) `Mail:ConvergenceProductNumber` doit être renseigné via `appsettings` avant la prod sinon X-MSS-NIL est omis avec un warning ; (2) `DraftService.BuildMailDto` ne résout pas encore `SaveDraftDto.AttachmentIds` en `MailDto.Attachments[].Content`. Annexe C enrichie avec task-001.

**Changelog v1.7** : passe tech-writer conservatrice. Task-017 (Impression et export d'un email — PDF / EML — avec traçabilité audit-trail des 3 actions distinctes `MailPrint`, `MailExportPdf`, `MailExportEml`) livrée sur `api-mail` (PR #29), `client-blazor` (PR #36) et `dtos-mss` (PR #8). Annexe C enrichie avec task-017.

**Changelog v1.6** : passe tech-writer conservatrice. Task-016 (Alignement fonctionnel Angular sur Blazor — 8 écarts UX/fonctionnels) livrée manuellement par le humain (repo `client-angular` entièrement exclu de l'automation forge). Annexe C enrichie avec task-016.

**Changelog v1.5** : passe tech-writer conservatrice (option A). Task-009 (Libellé expéditeur formaté selon ECO.2.2.7) a été livrée et fait passer RG-E009-043 de 🟡 Partiel à 🟢 Implémenté. Feature E009-F006 (Composition et envoi) passe de 95% à 97% (reste « annule et remplace » AMBU.MSS/va1.02). Annexe C enrichie avec task-009.

</details>

---

## Sécurité applicative — Détails techniques

> Ce chapitre complète §10 du doc produit (Motivation, choix UUID v7, Convention, Bilan défense en profondeur) avec les détails ingénierie : découpage tasks, audit grep, limites résiduelles techniques.

### Découpage en 3 tasks (018 → 019 → 020)

Le chantier a été séquencé pour limiter le blast radius :

| Task | Cluster | Entités migrées | NuGet `dtos-mss` |
|---|---|---|---|
| **task-018** | Patient + MailMedicalDocument | `MailPatient`, `MailMedicalDocument`, `MailMedicalDocumentBiology`, `MailMedicalDocumentSummary` (4 entités) | 231.0.0 |
| **task-019** | Mail + enfants | `Mail`, `MailContent`, `MailRecipient`, `MailAttachment`, `Tag`, `MailTag` (6 entités) + finalisation `MailMedicalDocument.MailId` | 235.0.0 |
| **task-020** | User / Contact / Audit + scellement | `User`, `UserSetting`, `MailSignature`, `MailTemplate`, `MailFolder`, `Contact`, `ContactMssAddress`, `ContactTag`, `ContactGroup`, `ContactGroupMember`, `MssAuditTrace`, `PendingAction` (12 entités) + finalisation `MailMedicalDocument.PractitionerContactId` + suppression `ContactDto.GetIntId()` | 239.0.0 |

**Total : 22 entités migrées**, 17 routes API `{*:int}` éliminées (MedicalDocuments 3, Contact 8, MailTemplate 3, Signature 5, Audit 1, MailController.CancelPendingEmail 1 — chevauchements sur le décompte exact entre les domaines).

**Stratégie « à la source »** : pas de cohabitation `int Id` interne + `Guid PublicId` externe. Les colonnes `Id` Postgres elles-mêmes deviennent `uuid`. Migration consolidée `20240101_SetupMigration.cs` éditée directement (convention "re-consolider plutôt qu'empiler", base dev-only) — pas de migration de backfill nécessaire.

### Bilan de scellement (audit grep)

Vérifications passées en sortie de task-020 (exécutables comme test d'anti-régression) :

| Vérification | Cible | Résultat |
|---|---|---|
| `grep -rE '\bint\s+Id\s*\{' Api/Mail/src/Domain/Entities/` | vide | ✅ |
| `grep -rE '\{(id\|contactId\|groupId\|patientId\|documentId\|mailId\|tagId\|templateId\|userId\|traceId):int\}' Api/Mail/src/Api/Controllers/` | vide | ✅ |
| `grep -rE '\.AsInt32\(\).*PrimaryKey' Api/Mail/src/Infrastructure/Migrations/` | vide | ✅ |
| `grep -rE 'public\s+(int\|long)\s+Id\s*\{' Dtos/` | vide | ✅ |
| `grep -rnE 'GetIntId' Dtos/ Api/Mail/src/ Client/Blazor/Src/` | vide (binaires .dll exclus) | ✅ |
| `grep -rnE 'BitConverter\.ToInt32.*ToByteArray' Dtos/` | vide | ✅ |
| `grep -rnE 'id\s*:\s*number' Client/Angular/front/libs/mss/src/core/models/` | uniquement IMAP UIDs | ✅ |
| `grep -rE 'UseIdentityAlwaysColumn' Api/Mail/src/Infrastructure/Persistance/MailDataContext.cs` | vide | ✅ |

**100% des PK Postgres sont en `uuid` v7**, **0 routes `{*:int}`**, **0 hack BitConverter**, **anti-énumération IDOR couvert sur l'ensemble des entités exposées**.

Audit grep complémentaires pour les couches 2 / 2bis / 3 (tasks 021 / 022 / 023) :

| Vérification | Résultat |
|---|---|
| `grep -rE 'TryExtractJwtToken' src/Api/Controllers/` (task-021) | ✅ vide |
| `grep -rE 'AllowAnonymous' src/Api/Controllers/V1/*.cs` (task-022) | ✅ vide |
| `grep -rE 'Headers\["Client-Email"\]' src/Api/Controllers/` (task-022) | ✅ vide |
| `grep -rE 'FromQuery.*email' src/Api/Controllers/V1/MailEventsController.cs src/Api/Controllers/V1/NotificationsController.cs` (task-022) | ✅ vide |
| `grep -rE 'FindAsync\(\[?id\]?\)' src/Infrastructure/Repository*` (task-023) | ✅ vide |
| `grep -rE 'FirstOrDefaultAsync\([^)]*\.Id == id\)' src/Infrastructure/Repository*` sans `&& UserId ==` (task-023) | ✅ vide |

### Limites résiduelles et roadmap technique

**Couche 2 (task-021) — Authentification cryptographique JWT** : `Pass 6` (tests sécurité HTTP-pipeline via `WebApplicationFactory<Program>` — spoofing `Client-Email`, JWT expiré, JWT forgé, SSE query token) deferred. Couvert opérationnellement par `/qa` Playwright en test-bypass mode. À ajouter en task dédiée si la stack grandit.

**Couche 2bis (task-022) — SSE et endpoints anonymes** : CORS policy `AllowAnyOrigin().AllowAnyMethod().AllowAnyHeader()` reste active. À durcir en prod via whitelist explicite des origins (dev reste permissif pour `/qa` et hot-reload). Tests E2E HTTP-pipeline toujours absents (carry-over task-021).

**Couche 3 (task-023) — Ownership scoping repositories** :
- `ContactController.AddToGroupAsync` retourne 400 (existing convention `Contact already in group or not found`) plutôt que 404 sur ownership KO. Le 400 ne leak pas l'existence (message générique conflate les cas), donc la propriété de sécurité tient — mais strictement la convention task-023 voudrait 404. Polish PR future.
- `GetCurrentUserIdAsync` est appelé une fois par méthode de repo, soit un round-trip Postgres supplémentaire par appel. La requête est indexée (`IX_Users_Email` unique) donc rapide ; opportunité de cache HTTP-scoped si la métrique latence remonte.

**Bilan global** : le seul vecteur IDOR résiduel identifié serait un compromis de la table `Users` elle-même (récupération du mapping email→Guid puis forge d'un JWT signé), ce qui suppose une compromission du Keycloak ou de la BDD — hors périmètre application, traité au niveau infrastructure (rotation des clés, accès BDD restreint, audit Seq).

---

## Annexes

### A. Cartographie des briques applicatives clés

| Composant fonctionnel | Briques applicatives | Règles couvertes |
|-----------------------|----------------------|------------------|
| Connexion IMAP | Service de connexion IMAP, gestionnaire de connexion, couche d'abstraction IMAP | RG-E009-001/002/004/006/007/008 |
| Connexion SMTP | Fabrique de connexion SMTP, service SMTP | RG-E009-003/005 |
| Sécurité TLS | Validateur de suites de chiffrement TLS, validateur de certificats, helper `ImapClientTlsConfigurer` (task-038) | RG-E009-002/013 |
| Révocation certificats | Services de vérification CRL et OCSP | RG-E009-012 |
| Auto-configuration | Service d'autoconfiguration DNS SRV | RG-E009-015 |
| Annuaire Santé | Service d'annuaire santé et ses 5 stratégies de recherche | RG-E009-021 à 026 |
| Traitement CDA / IHE_XDM | Moteur de traitement des documents CDA, moteur de traitement des archives IHE_XDM, bibliothèque d'interopérabilité CDA | RG-E009-029/032/055/056/057/058 |
| Construction CDA / IHE | Constructeurs de document CDA et de paquet IHE, générateur de paquet, générateur de métadonnées | RG-E009-018 |
| Rendu CDA | Service de transformation CDA, service d'export CDA | RG-E009-054/057 |
| Référentiels CDA | Référentiels intégrés (OID, LOINC, modèles, convertisseurs) | Conformité CI-SIS |
| Construction emails | Moteur de construction d'emails, utilitaires d'adresses | RG-E009-037/038/039/042/043 |
| Vérification taille PJ | Paramètre applicatif « taille maximale des pièces jointes », paramètres utilisateur | RG-E009-040 |
| Biologie | Moteur biologie, entités « résultat de biologie » et « état d'interprétation » | RG-E009-049/051/052/053/054 |
| Acquittement biologie | `BiologyAck` (5 actions médico-légales), `IBiologyAckService` (task-028) | — (driver médico-légal) |
| Notifications | Notifieur d'arrivée de message, canaux temps réel | RG-E009-051 (alertes biologie) |
| MDN / DSN | Gestionnaire d'accusés de lecture | RG-E009-041/042 |
| Journal d'audit | Journal d'audit MSS, API d'audit, énumération des 16 types d'action + 5 BiologyAck (task-028) + 3 MailPrint/Export (task-017) + 2 MailSuppression (task-015b) | RG-E009-045/046/047 |
| Opposition patient | Patient rattaché avec statut d'opposition MES et pro | RG-E009-019/020 |
| Brouillons / Envoi | Gestionnaire de brouillons, service SMTP | RG-E009-040 |
| Annule et remplace | `IMailCancellationService`, threading RFC 5322 (task-006) | — (mapping AMBU.MSS/va1.02 à formaliser) |
| Détection doublons / versions | `MailRepository.DetectDuplicateAsync` (task-013 puis INT.18 normative task-034 : Exact / NewVersion / Obsolete / None) | RG-E009-058 |
| Suppression CDA | `MailRepository.DetectSuppressionRequestAsync` (task-015a) + endpoint accept/refuse (task-015b) + navigation versions (task-015c) | LGC.MSS/UX.05 |
| Onboarding MSSanté | `IMssAccountOnboardingService`, `MssOnboardingGuard`, `Mss403Handler` (task-037), helper TLS `ImapClientTlsConfigurer` (task-038) | — (UX/sécurité onboarding) |
| Synchronisation en arrière-plan | Services de synchronisation en arrière-plan | RG-E009-070 |
| File d'attente hors ligne | File d'attente d'actions hors ligne | RG-E009-070 |
| Contexte utilisateur | Contexte utilisateur (jetons PSC, RPPS, profil), `UserContextEnricherMiddleware` (task-021) | RG-E009-014/063/064 |
| Sondes de santé | Endpoints `/health` (readiness, tags `ready`) + `/alive` (liveness, tags `live`), checks PostgreSQL / Redis / bus MassTransit (task-056) | — (observabilité production) |
| IA / Sémantique | Service de recherche sémantique, plugin d'actions métier, service de suggestion de tags IA, service de gestion des conversations IA | F013 |
| Vue patient | API Patients, API Biologie, Vue temporelle patient, Timeline biologie horizontale, Synthèse clinique | F004 |
| Widget patient | `PatientWidgetComponent` (5 derniers patients avec mails non lus, task-035) | F004 / dashboard |
| Gestion IMAP (dossiers, bulk, lu/non lu) | API de gestion des messages (endpoints dossiers), liste des dossiers, vue dossier, liste des messages, dialogue de déplacement en masse | F001 |
| Signature email | API de gestion des signatures, entité Signature, éditeur de signature | F014 |
| Modèles email IA | API de gestion des modèles, API d'assistance IA (génération, correction, amélioration, détection placeholders), entité Modèle, éditeur de modèle | F015 |
| Chat IA multi-emails | API de chat IA, service de gestion des conversations IA, plugin d'actions métier, panneau de chat IA | F013 |

### B. Inventaire fonctionnel (avril 2026)

- **Backend MSS** : 21 APIs HTTP versionnées (v1) — Chat IA, Assistance IA, Audit, Biologie, Connexion, Contact, Annuaire, Brouillon, Feature Flag, Messages, Événements mail, Modèles d'email, Administration, Notifications, Patients, Recherche, Paramètres, Signature, Synchronisation, Account (onboarding task-037), MedicalDocuments. Architecture en couches (Domaine / Application / Infrastructure / API). 5 projets de test (api 8, application 74, domaine 21, infrastructure 15, intégration 22 = **140 fichiers de test**).

- **Frontend embarqué** (dans le shell du LGC hôte) : **79 vues**, module messagerie unique. 5 widgets typés (biologie anormale, notifications mail, nouveaux messages, statut hors ligne, progression de synchronisation) + widget Patient (task-035). Vues clés :
  - **Vue patient (F004)** : Vue temporelle patient, Timeline biologie horizontale, Synthèse clinique.
  - **Gestion IMAP (F001)** : liste des dossiers, vue dossier, liste des messages, dialogue de déplacement en masse.
  - **Signature (F014)** : éditeur de signature.
  - **Modèles (F015)** : éditeur de modèle.
  - **Chat IA (F013)** : panneau de chat IA.
  - **Onboarding (task-037)** : `MssUnconfigured` + `MailSetup`.

- **Frontend autonome** (application web) : **127 vues**, bibliothèque messagerie organisée en cœur / fonctions / interface, fonctions livrées : audit, contacts, tableau de bord, accueil, mise en page, messages, patient, paramètres, signatures, modèles. Vues clés :
  - **Vue patient (F004)** : Timeline biologie, Synthèse clinique, Vue temporelle patient, Historique médical, Groupe de documents de timeline.
  - **Gestion IMAP (F001)** : liste des dossiers, liste des messages (avec sélection multiple).
  - **Chat IA (F013)** : panneau de chat IA.
  - **Signature (F014)** : vue signatures messagerie.
  - **Modèles (F015)** : vue modèles messagerie.
  - **Onboarding (task-037)** : `MssUnconfiguredComponent` + `MssSetupComponent` + `mssOnboardingGuard`.

- **Bibliothèque d'interopérabilité CDA** : 3 assemblies (noyau CDA, analyseur IHE_XDM, convertisseur de formats), couverture des règles CI-SIS (parsing, building, transformation XSLT, export HTML/Markdown).

- **Contrats partagés** : package partagé entre le backend MSS et le frontend embarqué. Le frontend autonome consomme les contrats via des types régénérés manuellement.

- **Entités métier principales (Domaine)** :
  - Signature (identifiant, nom, contenu HTML, par défaut, horodatages).
  - Modèle (identifiant, nom, catégorie, sujet, corps, par défaut, horodatages).
  - Trace d'audit MSS (30+ champs, depuis task-004).
  - Document médical rattaché, Patient rattaché (classement INS depuis task-010).
  - `BiologyAck` (5 actions médico-légales, task-028).
  - `MailMedicalDocument` enrichi : `SetId`, `SupersededByDocumentId` (task-034), `SuppressionRequestedAt`, `SuppressionRequestedByMailId`, `SuppressionAccepted`, `SuppressionRefused` (task-015a/015b).

- **Plugin IA** : plugin d'actions métier exposant 5 actions exécutables par l'IA via le moteur d'orchestration IA.

### C. Tasks ayant contribué à cet EPIC

| Task | Apport principal | Règles touchées |
|------|------------------|-----------------|
| done-task-002 | Masquage du préfixe `XDM/1.0/DDM+` dans l'objet | RG-E009-029 |
| done-task-003 | Opposition patient à l'envoi MSS pro et patient | RG-E009-019, 020 |
| done-task-004 | Journal d'audit MSS complet (16 actions, 30+ champs, export CSV) | RG-E009-045, 046, 047 |
| done-task-005 | Distinction visuelle messages pro / patient + extraction INS depuis libellé | RG-E009-028, 030 |
| done-task-008 | Vérification de la taille des PJ avant envoi (configurable, défaut 10 Mo) | RG-E009-040 |
| done-task-010 | Affichage prioritaire du PDF encapsulé + 1 ligne pour CDA R2 N3 + PDF | RG-E009-055, 056 |
| done-task-009 | Libellé expéditeur formaté selon ECO.2.2.7 (format `<Titre>_<Prénom>_<NOM>_<Entité>`, sanitization anti-injection d'en-têtes, 17 tests unitaires) | RG-E009-043 |
| done-task-016 | Alignement fonctionnel Angular sur Blazor — 8 écarts UX/fonctionnels (onglet Biologie dans mail viewer, BiologyComponent standalone, onglets dynamiques PatientTimeline, rendu HTML structuré MedicalDocumentModal, filtre DocumentType, timeline verticale, synthèse grille asymétrique, biologie matricielle + sparkline). Livré manuellement (client-angular exclu de l'automation forge). | — (iso-fonctionnalité frontends, F004) |
| done-task-017 | Impression et export d'un email (PDF / EML) avec traçabilité audit. 3 endpoints distincts (`/print`, `/export/pdf`, `/export/eml`) générant 3 traces audit séparées (`MailPrint`, `MailExportPdf`, `MailExportEml`). PDF généré via QuestPDF avec rendu Markdown→PDF pour les narratifs CDA. EML : bytes RFC 5322 bruts via `MimeMessage.WriteToAsync`. Boutons Imprimer + menu Exporter PDF/EML dans `MailDetailComponent` (Blazor) avec `data-testid` complets. Angular en cours côté humain (TFS). | — (extension audit déjà couvert par RG-E009-045/046/047 via task-004) |
| done-task-001 | En-têtes SMTP MSSanté `X-MSS-CODECDA`, `X-MSS-INS`, `X-MSS-NIL`, `X-MSS-MES` injectés au moment de l'envoi par un nouveau `MssanteHeaderService` câblé dans `SmtpService.SetMessageHeaders`. Le service décode l'archive `ihe_xdm.zip` côté serveur. 25 tests unitaires. Nouveau paramètre applicatif `Mail:ConvergenceProductNumber` ; nouvel opt-in `SaveDraftDto.BlockPatientReply`. | RG-E009-009, 010, 011, 016, 087 |
| done-task-014 | Options sélectives de parsing CDA : enum `[Flags] CdaParseOptions` (`Metadata` / `Biology` / `Summary` / `HtmlBody` / `Attachments` / `All`) + paramètre optionnel sur `ICdaParsingService.ParseIheXdmZip`. `MssanteHeaderService` bascule sur `CdaParseOptions.Metadata`. Élimine le bruit log `XSLT compile error` à chaque envoi. Backward-compatible. | — (dette technique observabilité) |
| done-task-011 | Indicateur visuel d'intégration des documents médicaux (LGC.MDV.06). DTO `MailMedicalDocumentDto.PatientId` exposé, agrégat `PendingIntegrationsCount` calculé côté serveur. Badges vert/orange dans inbox + par-document dans la vue détail. 9 nouveaux tests. Coverage api-mail 49.6 % → 50.2 %. | — (référence LGC.MDV.06 ; mapping à formaliser) |
| done-task-006 | **Annule et remplace** (AMBU.MSS/va1.02). Permet la republication d'une version corrigée. Subject `[Annule et remplace] {original}` avec préservation du marker IHE_XDM, threading RFC 5322, body mention, recipients copiés. Nouvelle colonne `Mails.IsCancelled`, `IMailCancellationService` (Application, callback testable), 2 endpoints (`GET cancel-and-replace-draft`, `POST cancel-and-replace`). Strike-through + badge `[ANNULÉ]` sur les frontends. 1733 / 0 failed (+18 nouveaux). | — (AMBU.MSS/va1.02 — mapping table §6 à formaliser) |
| done-task-026 | **UI toggle « Bloquer la réponse du patient »** — comble le gap UI de SC.MSS/CONF.21 (ECO.2.2.8). Checkbox dans le compose Blazor + Angular, visible uniquement quand `HasPatientRecipient(To, Cc, Bcc)` vrai. Reset side-effect. Backend / DTOs inchangés depuis task-001. Blazor : `BlockPatientReplyHelper` + `BlockPatientReplyHelperTests` (+16 unit tests). | RG-E009-016 — ferme l'écart implémentation/UI |
| done-task-024 | Observabilité orthogonale au chantier sécurité — instrumentation lock IMAP + fix log-level race `AddNewMail`. (1) `LogError → LogInformation` sur la race condition de fallback ; (2) slot `(operation, acquiredAt)` thread-safe sur `MailClientSession`, `GetCurrentLockHolder` exposé, catch enrichis avec `HolderOperation` + `HolderHeldMs`. Aucun changement de comportement. +5 unit + 2 integration tests. | — (observabilité) |
| done-task-023 | **Couche 3 du chantier sécurité — Ownership scoping repositories**, clôture définitive. Ajout `UserId Guid NOT NULL + FK + IX` sur 4 tables manquantes. `BaseRepository.GetCurrentUserIdAsync` factorisé. 5 dépôts scopés cumulativement par UserId. Convention scellée (404 sur ownership KO, jamais 403). +21 tests cross-tenant. 1708 / 0 failed. | — (durcissement sécurité transverse) |
| done-task-022 | **Couche 2bis du chantier sécurité — SSE & endpoints anonymes**. Retrait `[AllowAnonymous]` temporaire sur 5 controllers. SSE refactor : email exclusivement depuis claim JWT, `?email=` ignoré. Nouveau scrubber `ScrubQueryStringToken` (regex partial generated). +10 tests sécurité. | — (durcissement sécurité transverse) |
| done-task-021 | **Couche 2 du chantier sécurité — Authentification cryptographique JWT** (secure-by-default). `AddJwtBearer` Keycloak, `PolicyScheme JwtOrTestBypass`, `FallbackPolicy = RequireAuthenticatedUser`, `UserContextEnricherMiddleware`. Suppression de ~120 `TryExtractJwtToken` dans 23 controllers. Mode dev permissif. Token accepté en query string pour SSE. | — (durcissement sécurité transverse) |
| done-task-020 | **Clôture du chantier durcissement sécurité Guid v7** — phase 3. 12 entités migrées. Suppression du hack `ContactDto.GetIntId()` (BitConverter sur 4 bytes, ~1/65k birthday-collision). 17 routes API migrées vers `{*:guid}`. NuGet `HealthPlatform.Dtos.Mss 239.0.0`. Helper `TestGuid.From(int)`. Scellement final via audit grep. | — (durcissement sécurité transverse) |
| done-task-019 | Migration cluster Mail + finalisation `MailMedicalDocument.MailId` vers Guid v7 — phase 2. 6 entités migrées. NuGet 235.0.0. `IMailRepository.AddNewMail` `Task<int>` → `Task<Guid>`. Helpers `MailRepository` `Dictionary<int,…>` → `Dictionary<Guid,…>`. Impact UI minimal (MailDto utilise Uid IMAP). | — (durcissement sécurité transverse) |
| done-task-018 | Migration des PK du cluster Patient + MailMedicalDocument vers UUID v7 (RFC 9562) — durcissement sécurité, élimine l'énumération IDOR sur `/medical-documents/{id}`. Stratégie « à la source ». Nouveau `UuidV7ValueGenerator`. Migration consolidée éditée. 3 routes API migrées. 13 DTOs cluster migrés. NuGet 231.0.0. Découpage en 3 tasks (018 / 019 / 020). | — (durcissement sécurité transverse) |
| done-task-013 | Détection des doublons CDA — SC.CDA/INT.18. `IsDuplicate` + `DuplicateOfId` sur `MailMedicalDocumentDto`. `MailRepository.FindExistingDuplicateOfAsync` (DocumentId exact OU combo `Ins+Category+Date+Title`). Nouvel endpoint `POST /api/v1/medical-documents/{id}/duplicate-decision`. Badge orange « DOUBLON » + bannière dans le détail. *Note : remplacé par task-034 normative INT.18.* | — (référence SC.CDA/INT.18 ; mapping à formaliser) |
| done-task-012 | Rattachement patient par comparaison visuelle. Endpoints `GET /api/v1/patients/match` (scoring pondéré) + `POST /api/v1/medical-documents/{id}/attach-patient`. `PatientAttachmentDialog` + bannière amber dans `MailDetailComponent`. 4 service unit + 1 repo unit + 4 repo integration sur Postgres réel (Testcontainers). Aucun gap résiduel. | — (mapping LGC.MDV à formaliser) |
| done-task-027 | Vue conversation Angular (parité Blazor) — finition Angular pure. `mail-state.service.ts` : `mailViewMode` computed, signaux `expandedThreadMessageId` / `expandedThreadInfo`, méthodes `expandThread` / `collapseThread`. Mode code-only sur client-angular. 105 tests verts. | — (parité UX iso-fonctionnalité, F004) |
| done-task-029 | **Parité recherche Angular ↔ Blazor** (élagage volontaire 14 → 6 chips médicaux). Dropdown riche : 3 chips Statut, 6 chips Médical, 4 chips Période, panel avancé. Post-review fix race condition (Subject + switchMap). 115 tests verts. | — (parité iso-fonctionnalité F004) |
| done-task-031 | **Framework de tests d'intégration recherche (anti-bug)**. xUnit + Testcontainers `pgvector/pgvector:pg16`. 17 scenarios assertant la sémantique AND stricte de `SearchByFiltersAsync`. Conclusion validée : le backend a toujours fait du AND correct ; le bug task-029 était la race condition Angular. | — (outillage qualité) |
| done-task-032 | **Couverture TU api-mail + Sonar iter-1** — filet de sécurité avant cleanup massif (task-033). 106 nouveaux tests, ~2000 LOC. Décision PO (Option A4) : `[ExcludeFromCodeCoverage]` sur 9 classes intractables. Coverage finale : line 70.2 %, branch 58.0 %. /sonar iter-1 : 9 issues fermées, 6 règles. | — (dette technique / outillage qualité) |
| done-task-034 | **SC.CDA/INT.18 — détection de doublons CDA version-aware** (id, setId, versionNumber). Remplace l'algorithme task-013. 4 états dérivés : Exact / NewVersion / Obsolete / None. `MailRepository.DetectDuplicateAsync`. UI badge "REMPLACÉ". NuGet 258.0.0. | RG-E009-058 |
| done-task-015a | **LGC.MSS/UX.05 — backend détection des messages de suppression pure (socle data)**. Algorithme `MailRepository.DetectSuppressionRequestAsync` + 5 garde-fous. 4 nouveaux fields sur `MailMedicalDocument`. NuGet 264.0.0. User-invisible par construction. | LGC.MSS/UX.05 (socle data) |
| done-task-015b | **LGC.MSS/UX.05 — endpoint accept/refuse + bannière UI suppression**. Endpoint `POST /api/v1/medical-documents/{id}/suppression-decision`. Service avec audit trace. Bannière Blazor + Angular avec optimistic UI. NuGet 269.0.0. +7 backend tests + 3 bUnit. | LGC.MSS/UX.05 (UI) |
| done-task-015c | **LGC.MSS/UX.05 — navigation cliquable entre versions de document CDA** (forward + reverse). `VersionChainDto` + endpoint `GET /api/v1/medical-documents/{id}/version-chain`. Badge "REMPLACÉ" cliquable + lien "Version précédente". NuGet 273.0.0. | LGC.MSS/UX.05 (navigation) |
| done-task-036 | **Bug fix sur task-015c — navigation cliquable**. La US 015c avait expédié avec `NavigationManager.NavigateTo("/Mail/{uid}")` côté Blazor et `Router.navigate(['/mail', uid])` côté Angular — routes inexistantes. Refactor : `IFolderEventService.SelectMailByIdAsync` (Blazor) et `MailEventService.requestSelectByUid$` (Angular). Tests durcis pour assertions sur le résultat. | — (bug fix sur 015c) |
| done-task-033 | **Cleanup Sonar massif api-mail** — 1 itération (fixes mécaniques safe épuisés). Code smells 166 → 124 (-42, -25%), hotspots 6 → 5. 18 règles traitées sur 38 fichiers. Dépendait de task-032 comme oracle de non-régression. | — (dette technique Sonar) |
| done-task-032ter | **Mock IMAP server — Approche A : extraction interface `IMailClientSessionManager`**. 5 consumers mis à jour. Retrait `[ExcludeFromCodeCoverage]` sur 3 services IMAP (~2836 LOC réintégrées). 57 tests IMAP existants adaptés. | — (outillage qualité / interface extraction IMAP) |
| done-task-032quater | **Samples CDA zip files pour tester CdaParsingService**. 5 zips IHE-XDM anonymisés + 1 malformed dans `tests/.../Resources/cda-samples/`. 8 tests d'intégration `CdaParsingIntegrationTests`. Retrait `[ExcludeFromCodeCoverage]` sur `CdaParsingService` (382 LOC). | — (outillage qualité / harness CDA) |
| done-task-032bis-fhir-mock | **Mock FhirClient HTTP — Chantier 2 du harness**. Ctor `internal AnnuaireSanteService(FhirClient, ...)`. `FhirMockHandler` + `FhirBundleFactory`. 14 tests stratégies + 8 tests SearchAsync. Harness réutilisable. | — (outillage qualité / harness réutilisable) |
| done-task-035 | **Widget Patient (5 derniers patients avec mails non lus)** — nouveau widget vertical sur le dashboard MSS. Endpoint `GET /api/v1/Patients/with-unread-mails?limit={int}`. Cloisonnement par UserId via base PostgreSQL par utilisateur. NuGet 279.0.0. Refresh temps réel via `IMailHubService` (Blazor) / `MailEventsStreamService` (Angular). +5 unit + 6 integration + 5 bUnit + 8 Vitest. | — (F004 / dashboard MSS) |
| done-task-028 | **Workflow d'acquittement bio anormale (médico-légal)**. Table append-only `BiologyAck` (PK Guid v7, FK Users + MailMedicalDocuments cascade). 5 actions (`Acknowledged`, `PatientCalled`, `PatientSummoned`, `ReferredToColleague`, `MarkResolved`). État dérivé `Pending` / `InProgress` / `Resolved`. Rôle `Doctor` enforced au niveau service. NuGet 283.0.0. +14 unit + 7 bUnit + 3 Vitest. | — (driver médico-légal, complète §6.13 / RG-E009-051-052) |
| done-task-037 | **MSSanté account onboarding** — opt-in flow quand le claim `mssEmail` est absent du JWT. Écran `MssUnconfigured` + `/mss/setup`. Endpoint `POST /api/v1/account/mss-imap-test`. `AllowMissingMssEmailAttribute` marker. NuGet 289.0.0. +18 backend + 20 Blazor + 20 Angular Vitest. | — (UX / sécurité onboarding) |
| done-task-038 | **Fix TLS callback manquant sur la sonde `POST mss-imap-test`** (task-037). En déploiement Linux, MailKit retombait sur la validation OS qui rejette la chaîne IGC-Santé. Extraction `ImapClientTlsConfigurer` (sealed, scoped) qui centralise `SslProtocols = Tls12` + `ServerCertificateValidationCallback`. `CertificateValidator.ValidateAsync` marqué `virtual`. +10 tests. | — (bug fix sur la sonde opt-in task-037) |
| done-task-048 | **Cross-check d'identité à deux facteurs PSC ↔ Keycloak — phase 1 observation**. Réponse à l'incident de production 2026-05-19 (mailbox d'un autre praticien affichée après changement de carte CPS sur même navigateur). Nouveau check dans `UserContextEnricherMiddleware.ApplyAuthenticatedUserAsync` : `KC.mssSub == PSC.sub` ET `KC.mssRpps == PSC.SubjectNameID` (Ordinal strict, casse exacte sur `SubjectNameID` capital D). Feature flag `PscIdentityOptions.Enforce` (config `PscIdentity:Enforce` + env `MSS_ENFORCE_PSC_IDENTITY`). Mode observation par défaut : logge EventId 3721/3722/3723 avec champs anonymisés (jamais de token complet), laisse passer ; mode enforcement : 403 + `{"error":"Identity check failed."}`. KC-only (offline) : skip cross-check. +16 unit + 5 integration tests. Suite api-mail 2018 / 0 failed. Phase 1 sans dépendance ; activation enforcement après task-049 + task-050. | — (defense-in-depth sécurité applicative, orthogonale aux REM/Ref#2) |
| archived-task-049 | **Opt-in `mssSub`/`mssRpps` côté backend-auth (proxy Keycloak)** — prérequis runtime de l'enforcement task-048. Livré hors automation forge directement dans le proxy Keycloak via WindSurf. Le flux d'opt-in MSSanté existant (PUT `/v1/admin/mss-profile` avec `{email}` seul) extrait désormais `sub` et `SubjectNameID` du PSC token de la session serveur et les persiste comme attributs utilisateur `mssSub`/`mssRpps`. Contrat de sécurité : aucune lecture depuis le payload client, refus strict de re-binding incohérent (409 + log Warning), idempotence sur valeurs identiques, casse stricte `SubjectNameID`. **Side audit** : aucun changement requis côté `client-angular` (le service onboarding posait déjà `{email}` seul depuis task-037) ni côté `api-mail` (consommation des claims déjà câblée par task-048). | — (defense-in-depth sécurité, prérequis enforcement task-048) |
| archived-task-050 | **Protocol Mappers Keycloak `mssSub`/`mssRpps`** — prérequis runtime de l'enforcement task-048. Livré hors automation forge directement dans le realm Keycloak via WindSurf (`devops` hors automation). Deux mappers `User Attribute` sur le scope MSSanté du realm, projetant `mssSub` et `mssRpps` (Claim JSON Type `String`, `Add to access token = true`, `Add to userinfo = true` pour debug). **Side audit** : aucun changement requis côté `client-angular` (les mappers sont pure config server-side) ni côté `api-mail` (`FindFirstValue` consomme automatiquement les nouveaux claims dès qu'ils apparaissent dans le JWT). | — (defense-in-depth sécurité, prérequis enforcement task-048) |
| done-task-054 | **Retrait du champ RPPS de l'opt-in MSSanté** (Blazor + Angular). Cleanup UX purement subtractif rendu possible par task-049 (le proxy Keycloak extrait désormais `mssRpps` server-side du PSC token). Blazor : retrait du bloc `<InputText>` + propriété `Rpps` + annotations `[Required]` / `[RegularExpression]` + 2 clés Localizer FR+EN. Angular (code-only) : retrait du `RPPS_PATTERN`, du contrôle `rpps` du `setupForm`, du bloc HTML et de l'utilitaire `rppsInput` du spec. Assertions anti-régression ajoutées des deux côtés. Aucun changement de contrat HTTP. Tests : Blazor 86/88, Angular 183/183, lint 0/0. | — (cleanup UX, dead-code) |
| done-task-055 | **Harmonisation de la gestion d'erreurs API via `ProblemDetails` (RFC 7807)**. Nouveau `GlobalExceptionHandler` (`IExceptionHandler`) produisant `application/problem+json` avec `traceId`, mapping **par type** (499/400/404/409/503/500) ; 4 exceptions métier typées (`NotFoundException`/`ValidationException`/`ConflictException`/`UnavailableException`) ; `AddProblemDetails` + `AddExceptionHandler` + `UseExceptionHandler` en aval de `RequestLoggingMiddleware` ; heuristique de mots-clés de `ResultExtensions.MapStatusCode` supprimée (mapping par `ResultStatus`, fallback 500) ; `BiologyController` + `ContactController` migrés (try/catch retiré, dé-`[ExcludeFromCodeCoverage]`). Détail client générique sur 5xx/499 (zéro fuite stack/exception/donnée de santé), corrélation logs par `traceId`. Blazor : `ProblemDetailsParser` + `HttpRequestService` status-first. Angular (code-only) : modèle `ProblemDetails` + `mapProblemDetailsToMessage`. Sonar new code 0 violation. PR #78 (api-mail), #55 (client-blazor). `CLAUDE.md` règle absolue 12 (ProblemDetails obligatoire). | — (robustesse / observabilité, hors couloir Ségur) |
| done-task-059 | **Uniformisation du path `Result` sur `ProblemDetails` (RFC 7807)** — keystone post-task-055 : `ResultExtensions.ToActionResult` (2 surcharges, `src/Api/Helpers/ResultModelExtensions.cs`) produit `application/problem+json` (`status`/`title`/`detail`/`type` + `traceId`/`instance`) au lieu du legacy `ErrorResponse(Success, Message)` (record supprimé, audit grep 0 référence). Mapping par `ResultStatus` task-055 inchangé, `LogServerFailure` préservé, `detail` 500 générique (zéro fuite), `Result.Invalid` → `ValidationErrors`. Tous les controllers au pattern `Result` émettent du `problem+json` sans modification individuelle — déverrouille les tasks 060–065 (retrait du boilerplate try/catch). 30 tests `ResultExtensionsTests` (test-first RED→GREEN) ; Sonar new code 0 violation (CA1859 fixé), fichier 95.7 % new-coverage. PR #79 (api-mail). | — (robustesse / observabilité, hors couloir Ségur) |
| done-task-081 | **Timing par commande IMAP dans `GetFolderQuery`** — chaque commande (STATUS, SELECT, SEARCH, CLOSE) chronométrée individuellement : 4 tags d'Activity (`imap.*_ms`) + log `[ListFolder]` enrichi des 4 durées, ventilation requêtable dans Seq. Diagnostic des listages lents (~68 s observés) : la prochaine reproduction nomme la commande coupable. Aucun changement de flux ; 4 tests test-first dont capture des tags via `ActivityListener`. Inclut le fix S138 `CdaParsingService` (héritage commit « Add metrics »). PR #103 (api-mail). | — (observabilité technique, hors couloir Ségur) |
| done-task-085 | **Correcteur orthographique FR (local)** — `spellcheck="true" lang="fr"` sur le compositeur (corps + objet), correction navigateur native **strictement locale** (aucun service tiers, anti-exfiltration données de santé). Blazor RadzenHtmlEditor/RadzenTextBox ; Angular TipTap `editorProps.attributes`. Tests de composant des 2 fronts (Blazor rendu complet NewMailComponent ; Angular editorProps). Sonar skip. PR #58 (blazor), Angular code-only. | — (confort de rédaction, hors couloir Ségur) |
| done-task-086 | **Import/export du carnet d'adresses (vCard `.vcf`)** — `VCardSerializer` sans dépendance (écrit 3.0, lit 3.0/4.0 tolérant), `VCardService` import **anti-doublon** (RPPS puis adresse MSSanté sur snapshot unique, jamais de clone), `VCardImportReport` (DTO local par repo → dtos-mss vide). Endpoints `GET Contact/export/vcard` (streamé) + `POST Contact/import/vcard` (multipart, malformé→400 ProblemDetails). Blazor boutons Export/Import + toast compte-rendu (i18n EN+FR) ; Angular `MssApiService.exportVcard/importVcard` + `mss-contacts` (FR en dur). Tests : 14 unit + 5 controller + 3 intégration Postgres (api-mail), 2 bUnit (blazor), 1 spec + 215 mss-lib (angular). PR #105 (api-mail) + #59 (blazor), Angular code-only. | — (gestion du carnet d'adresses, hors couloir Ségur) |
| done-task-084 | **Rappel de pièce jointe oubliée** — à l'envoi, si le corps/objet évoque une PJ (ci-joint, pièce jointe, PJ, en annexe, veuillez trouver) et 0 fichier attaché → confirmation « Envoyer quand même ? ». Détection FR configurable, insensible casse/accents, strip HTML, PJ token isolé. Front-only : Blazor `AttachmentReminderDetector` + `NewMailComponent` (i18n) ; Angular util pur + `mail-compose` (dialogue inline réutilisé). 13+13 tests unitaires. Sonar skip. PR #57 (blazor), Angular code-only. | — (confort de rédaction, hors couloir Ségur) |
| done-task-083 | **Téléchargement groupé des pièces jointes (ZIP)** — endpoint streaming `…/attachments/download/zip` (`application/zip`, jamais sur disque), désambiguïsation des doublons, 404 ProblemDetails si aucune PJ, trace PGSSI-S sans nom de fichier. Bouton « Tout télécharger (ZIP) » (≥ 2 PJ) Blazor + Angular. 🔴 Bug Kestrel `AllowSynchronousIO` sur `ZipArchive.Dispose()` attrapé/fixé par l'humain (buffer `FileBufferingWriteStream` + drain async) — non détecté par le test controller sur MemoryStream. Tests : 6+3 backend, 4 bUnit Blazor, 199 Angular. Sonar QG OK, 0 new-code. api-mail sur develop (`5cd430a`+`9b6abbf`) ; PR #56 (blazor) ; Angular code-only. | — (confort utilisateur, hors couloir Ségur) |
| done-task-082 | **Campagne de couverture de tests backend** (objectif initial 90 %, **rescopé 85,4 %** au `/review` — gain figé) — 84,4 % → **85,4 %** (+1,0 pt), +57 tests nets, zéro fichier `src/` modifié. 7 fichiers : `BaseEmbeddingProviderService` (troncature/erreur), boucles hébergées `BackgroundImapConnectionMaintenanceService`/`MailClientSessionCleanupService` (0 %→couvert), happy-paths `ImapFolderService` (create/rename/delete/move/bulk/self-heal/cache), branches `ImapConnectionService` (domaine inconnu/token vide/réutilisation + 503 connectivité), draft/sent/tag/thread `ImapService`, intégration Testcontainers `MailRepository` (12 tests). `BaseRepository`/`SemanticSearchRepository` replanifiés hors task (déjà couverts transitivement ; restant = branches chirurgicales + extension methods MailKit non-mockables). 🟡 Bug latent : `MarkReadReceiptSentAsync` (`ReadReceiptSentAt` `timestamptz` EF vs `timestamp` FluentMigrator, cf. task-078) → task EF dédiée à planifier. PR #104 (api-mail). | — (outillage qualité, hors couloir Ségur) |
| done-task-078 | **Fix courrier patient avec INS** — résout le 🔴 finding v1.36 : `CreatedAt = NormalizeUtc(DateTime.UtcNow)` (Kind=Unspecified) dans le chemin de promotion patient-courrier ; tout `AddNewMail` d'un mail patient Mon Espace Santé avec INS persiste à nouveau (document COURRIER + rattachement patient task-005 rétablis). Balayage : `ReadReceiptSentAt` corrigé à l'identique (même classe de bug). 2 tests d'intégration Postgres RED→GREEN, INS factices. PR #100 (api-mail). | SC.MSS/UX.25 · ECO.3.1.1, SC.MSS/UX.31 · ECO.3.1.2 (correctif V2, RG-E009-028/030 rétablies) |
| done-task-067 | **Campagne itérative de couverture de tests** (`code-coverage-skill`) — 77.4 % → **83.3 %** (+5.9 pts), Quality Gate Sonar OK toutes conditions vertes (artefact `new_coverage` résorbé), +69 tests nets, zéro fichier `src/` modifié. 5 itérations + clôture : RedisSyncStateStore (0 %→couvert), EmailSummaryService/AiConversationService/SemanticSearchService (chemins IA via kernel substitué), PendingActionService, AddNewMailConsumer, MailRepository (tags/drafts/threads/promotion via Testcontainers), MailMaintenance + AiDiagnostics controllers (downcast `DataContext`). 🔴 Finding : bug latent `AddPatientCourrierDocument` (UtcNow → timestamp sans tz, DbUpdateException sur tout mail patient avec INS) → fix dédié à planifier. PR #89 (api-mail). | — (outillage qualité, hors couloir Ségur) |
| done-task-058 | **Durcissement `MailClientSessionManager`** — Finding B : `CleanupExpiredSessions` récupère les sémaphores des sessions expirées (folder locks + email-fetch locks, ces derniers seulement quand la dernière session de l'email disparaît — garde `HasActiveSessionsForEmail`), correction de l'oubli symétrique `_emailFetchLocks` dans `CleanupLocksForSession`. Finding C : try/catch à l'intérieur du while de `RunCleanupTaskAsync` (un sweep raté ne tue plus la boucle), sortie propre sur cancel, cadence interne `CleanupInterval`. 6 tests test-first, hooks internes DOD. PR #88 (api-mail). | — (robustesse / disponibilité plateforme, hors couloir Ségur) |
| done-task-057 | **Suppression des `new HttpClient()` OCSP** — `OcspValidationService` injecte `IHttpClientFactory`, client nommé `"OcspClient"` enregistré dans `AddApi` (miroir `CrlClient`) : sockets poolées (anti `TIME_WAIT`), resilience handler par défaut + instrumentation OTel HttpClient sur les requêtes OCSP. Iso-comportement de validation (révoqué rejeté, prouvé par test). `[ExcludeFromCodeCoverage]` retiré — harness responder (CA + AIA réelle + réponses OCSP BouncyCastle signées), 12 tests, fichier 95.8 % (new_uncovered_lines = 0). PR #87 (api-mail). | — (robustesse / fiabilité plateforme, hors couloir Ségur) |
| done-task-092 | **Durcissement de la politique CORS (S5122)** — US mono-repo api-mail. `AllowAnyOrigin()` retiré du code ; `CorsPolicySetup.AddMssCors` applique toujours `WithOrigins` (whitelist `Cors:AllowedOrigins` configurable dev/prod) ; prod sans config = refus total, dev = allowlist localhost ; jamais de wildcard. Tests : 3 unit (résolution origines) + 3 TestServer (autorisée→ACAO, refusée, pas de `*`). PR #110. | — (PGSSI-S restriction des origines, suivi S5122, hors REM spécifique) |
| done-task-091 | **En-têtes de sécurité HTTP** — US mono-repo api-mail. `SecurityHeadersMiddleware` (Response.OnStarting, survit à l'exception handler) : CSP restrictive (Swagger préservé), X-Frame-Options SAMEORIGIN, X-Content-Type-Options nosniff, Referrer-Policy, HSTS (HTTPS only). `SecurityHeadersOptions` configurable dev/prod. CSP = 2ᵉ barrière anti-XSS (complète task-088) ; sans impact SPAs. Tests : 3 TestServer (en-têtes + HSTS https/http). PR #109. | — (PGSSI-S durcissement applicatif, hors REM spécifique) |
| done-task-090 | **Limitation de débit des requêtes API (rate limiting)** — US mono-repo api-mail. `AddRateLimiter`/`UseRateLimiter` après auth, partition par identité PS (repli IP) ; 429 `application/problem+json` + `Retry-After` ; seuils configurables (`RateLimiting` appsettings/env) ; policy stricte `sensitive` ; no-op si `Enabled=false`. `RateLimitingOptions`+`RateLimitingSetup` extraits/testables. Tests : 4 unit (partition/isolation) + 2 TestServer (200, 429+Retry-After+problem+json). PR #108. | — (PGSSI-S disponibilité / anti-abus, hors REM spécifique) |
| done-task-089 | **Blocage du contenu distant des courriers (anti-pistage)** — US frontend-only (api-mail/dtos vides). Neutralise `src`/`background`/`srcset` http(s) + `url()` CSS distant avant l'injection dans l'iframe sandboxée (compose avec task-088) ; `cid:`/`data:` préservés ; bannière « images bloquées » + « Afficher les images » (ce courrier uniquement, sans mémorisation). Blazor helper pur `RemoteContentBlocker` + `MailBodyComponent` ; Angular util `remote-content.util` + `mail-body` (FR en dur). Tests : 9+2 (blazor), 7+3 + 233 mss-lib (angular). PR #62 (blazor), Angular code-only. | — (PGSSI-S confidentialité / anti-accusé-de-lecture, hors REM spécifique) |
| done-task-088 | **Assainissement HTML des courriers (anti-XSS)** — `HtmlBodySanitizer` allowlist sur AngleSharp 1.5.0 (HtmlSanitizer/Ganss écarté : pin AngleSharp 0.17.x + advisory NU1902). Assainit le corps HTML avant stockage (`EmailBuildingService`) et avant exposition (`ImapService.GetEmailContentAsync`) : retire script/`on*`/`javascript:`/`data:` exécutable/iframe/object/embed, CSS expression/behavior ; préserve titres/listes/tableaux/liens/images ; CDA (chemin séparé) non régressé. Blazor `MailReadOnlyView` → iframe sandboxée (plus de MarkupString brut) ; Angular `mail-read-only-view` → `medical-html-frame` (plus de `bypassSecurityTrustHtml`). Tests : 12 unit + 1 intégration (api-mail), 2 bUnit (blazor), 2 spec + 223 mss-lib (angular). PR #107 (api-mail) + #61 (blazor), Angular code-only. | — (PGSSI-S intégrité / anti-code-malveillant, hors REM spécifique) |
| done-task-087 | **Affichage du quota d'occupation de la boîte MSSanté** — `MailboxQuotaDto` (NuGet 327.0.0) ; backend `MailboxQuotaService` lit l'extension IMAP QUOTA (RFC 2087) via `IImapClientWrapper.GetMailboxQuotaAsync` (`Inbox.GetQuotaAsync()`, STORAGE KiB→bytes), endpoint `GET account/quota` (réutilise `IImapConnectionService`) ; serveur sans QUOTA → `Available=false` (200), connexion KO → 503 ProblemDetails (règle 12) ; log du seul pourcentage (zéro donnée santé). Blazor `MailboxQuotaFooterComponent` (jauge linéaire pied de `FolderListComponent`, ambre ≥80 % / rouge ≥90 %, « Quota non disponible ») ; Angular `MailboxQuotaWidgetComponent` (patron `SyncProgressWidget`, FR en dur, code-only). Tests : 5+2 unit + 2 intégration (api-mail), 6 bUnit (blazor), 6 spec + 221 mss-lib (angular). Sonar skip (infra absente). PR #24 (dtos) + #106 (api-mail) + #60 (blazor), Angular code-only. | — (supervision de la boîte, hors couloir Ségur) |
| done-task-056 | **Sondes de santé production (readiness/liveness)** — `/health` et `/alive` mappés dans **tous** les environnements (garde `IsDevelopment()` retiré de `MapDefaultEndpoints`), `.AllowAnonymous()` (FallbackPolicy task-021), réponse = statut agrégé seul (zéro fuite). `/health` agrège les checks `ready` : PostgreSQL (`AddNpgSql`, connexion serveur `MSS-MAIL-CONNECTIONSTRING`), Redis (`AddRedis` sur l'`IConnectionMultiplexer` Aspire), RabbitMQ via le bus health check MassTransit (auto-enregistré `ready`+`masstransit`, contrat épinglé par test) ; `/alive` limité au check `self` (`live`). Packages `AspNetCore.HealthChecks.NpgSql`/`.Redis` 9.0.0. Exclusion tracing OTel inchangée. 6 tests d'intégration TestServer env Production (200/503, leak-guard). PR #86 (api-mail). | — (robustesse / observabilité, hors couloir Ségur) |
| done-task-093 | **Glisser-déposer des courriers (Angular)** — US Angular-only (api-mail/dtos non touchés ; geste UI sur endpoints move/delete existants). Drag & drop natif HTML5 + `MailDragService` (signaux, valide la cible pendant `dragover`) ; règles dans la fonction pure `evaluateMailDrop` (cibles {Inbox,Custom,Junk,Trash} ; `*→Sent/Drafts/All/Tag` interdit ; `Sent`/`Drafts`→Trash only ; `Trash→Junk` interdit ; `Trash→Inbox/Custom`=restauration ; no-op même dossier). Suppression = deferred-delete style Gmail (`MailPendingDeleteService` : toast « Annuler » ~6 s sans appel backend ; flush réel `deleteEmail`/`bulkDeleteEmails` à l'expiration/teardown) — contourne le nouvel UID IMAP au restore, la cascade lien-patient ne part qu'après la fenêtre. Move/restore optimistes + rollback snackbar. 🔴 Bug attrapé au /review (corrigé) : tombstone 90 s non levé à l'undo → `MailStateService.clearTombstones` + 3 tests régression. Tests : `nx test mss-lib` 288/288 (53 task-093). Code-only (TFS). | — (ergonomie de gestion de la boîte, hors couloir Ségur ; journalisation/habilitations/cascade héritées du backend) |
| done-task-094 | **Optimisation du déplacement d'emails (UX fluide + réduction charge serveur)** — US backend+frontend (sans Blazor). **Backend** : `IMailRepository.RekeyMovedMailsAsync` + record `MailUidRemap` — après un MOVE IMAP UIDPLUS, rekey **en place** des lignes locales (`FolderPath + Uid`) au lieu du delete + ré-enrichissement du background sync ; **préserve** lien patient / documents / tags / contenu (enfants liés au `Mail.Id` immuable) ; atomique (1 `SaveChanges`), idempotent, sûr en concurrence (drop de la ligne source si la cible existe déjà). `ImapFolderService` single+bulk câblent le rekey depuis la map UIDPLUS, best-effort (`TryRekeyMovedMailsAsync`) ; échec IMAP → aucune mutation DB ; cache/API/DTO inchangés. **Frontend (code-only)** : `MailDestinationRefreshService.refreshAfterMove` (retries bornés 300/1200/3000 ms, arrêt anticipé, pas de polling global) + `MailStateService.applyFolderMetadataByPath` (MAJ métadonnées dossier cible, tombstone-safe) ; déclenché dans `applyMoveSuccess`, sans régression move/undo. Tests : `MailRepositoryRekeyTests` 7/7 (backend) ; `nx test mss-lib` 298/298 (10 task-094, frontend). Full-suite api-mail : 2 échecs pré-existants flaky (passent en isolation), pas une régression. Sonar skip (infra absente). PR #111 (api-mail) ; `dtos-mss` branche vide (pas de PR) ; Angular code-only (TFS). | — (optimisation perf/ergonomie d'un flux existant, hors couloir Ségur ; aucun nouveau flux MSSanté/CI-SIS) |

| archived-task-153 | **« Répondre à tous » (Angular + mobile)** — US frontend-only bi-repo (`api-mail`/`dtos-mss` non touchés, envoi via le `sendMail` existant). Manque **commun aux deux clients** relevé par l'analyse différentielle du 2026-07-05, pas un écart de parité. Util **pur** `reply-all-recipients.util.ts` (spécification unique, deux implémentations TS) : **À** = `Reply-To` sinon `From`, + les `To` d'origine **moins l'adresse du praticien** (jamais d'auto-adressage) ; **Cc** = `Cc` d'origine moins le praticien ; **aucun `Cci` repris** ; casse/espaces normalisés. Citation / « Re: » / threading RFC-5322 réutilisent la mécanique « Répondre » existante. Action **visible seulement si > 1 correspondant** après filtrage (même règle sur les 2 clients). FR en dur, `data-testid`. Tests : mobile 522 ; Angular MSS 312 + lint scope MSS 0 erreur. ⚠️ Conflit sémantique `ComposeRequest.to` avec task-152 (`string[]` vs `MailAddressDto[]`) réconcilié en staging → `toEmails`, appliqué au merge du 2026-07-17. PR #55 (mobile, mergée `6211ecb`) ; Angular code-only (TFS). | — (complément d'une capacité d'émission déjà référencée, hors DSR nouvelle ; en-têtes MSSanté, journalisation PGSSI-S et habilitations héritées) |
| done-task-175 | **Cloisonnement par praticien du flux SSE mail (fuite inter-praticiens)** — US backend-only api-mail ; contrat SSE inchangé, aucun frontend modifié. `SseMailEventBroker` (singleton) était clé sur le **seul nom de dossier**, identique pour tous : une publication pour la boîte d'un praticien était écrite dans le canal SSE de **tous** les autres abonnés du même dossier (sujet, expéditeur, contenu, documents CDA, `PatientInsMatricule`) — le fan-out ayant lieu en mémoire, l'isolation base-par-praticien ne protégeait pas. Clé devenue un `MailboxFolderKey(mailbox, folder)` normalisé (casse/trim conservés) ; `Remove`/`SubscriberCount`/boucle CAS portés dessus (pas de bucket orphelin). Identité = claim JWT `mssEmail` validé côté abonnement (**contrat task-022 préservé**, `?email=` ignoré) et contexte de la boîte réellement synchronisée côté publication (`userContextInfo.Email`, `message.UserContext.Email`) — **jamais** un `IHttpContextAccessor`. **Fail-close** : publication sans identité → aucun abonné servi + `Warning`. Journal **pseudonymisé** (`MailboxPseudonym`, SHA-256 tronqué) : ni adresse MSSanté, ni sujet, ni INS, ni contenu. `[ExcludeFromCodeCoverage]` retiré. Tests : 23 unitaires + 4 intégration (assertions sur les octets écrits dans le corps de réponse de chaque praticien) + 2 contrôleur ; **non-vacuité prouvée** (6 échecs sur la clé pré-fix). Sonar : 3 findings S103 introduits, tous corrigés ; QG `ERROR` dû à 3 violations Python **antérieures** hors diff. **AIPD / notification CNIL à qualifier avec le DPO** (livrable humain distinct). PR #126. | — (correctif de conformité MSSanté / PGSSI-S confidentialité & contrôle d'accès, vague V2 volet MSSanté ; aucune nouvelle exigence adressée) |

### D. Sources documentaires

#### Sources primaires (lues intégralement pour v1.1 de cet EPIC)

- **Référentiel socle MSSanté #2 — Clients de Messageries Sécurisées de Santé**, ANS, version 1.0.1 du 18/01/2024, 57 pages. Fichier : `docs/Referentiel/MSSANTE/ANS_MSS_Ref2_Clients_de_messageries_MSSanté_v1.0.1_20240118-vYRF3G7r.pdf`. Définit **34 exigences `ECO.*` obligatoires** pour les BAL personnelles/organisationnelles (§ 6.1.1) et les exigences complémentaires pour BAL applicatives (§ 6.1.2). Base de la conformité technique MSSanté.

- **Eléments d'information à destination des éditeurs de solution MSSanté pour les professionnels — ENS Mon espace santé Messagerie V1.3**, Assurance Maladie / CNAM Dionis, 28/06/2023, 15 pages. Fichier : `docs/Referentiel/MSSANTE/ENS-Mon_espace_sante_-_Messagerie_V1.3.pdf`. Précise les **comportements spécifiques à MES** : adressage mineurs, bounces, fin d'échange, MDN RFC 8098, `reply-to`. Base de E009-F007.

#### Sources secondaires (contextuelles, non ré-extraites)

- `docs/synthese-direction-messagerie.md` (2026-04-11) — synthèse direction produit, indicateurs de couverture par phase et par persona.
- `docs/analyse-conformite-messagerie.md` (juillet 2025) — analyse fonctionnelle vs spécification NOVA + matrice des 72 exigences REM Ségur, cartographie fonctionnelle. Base de la **section 6.1 à 6.14** du doc produit.
- `docs/Referentiel/MSSANTE/Guide_de_mise_en_oeuvre_MSSante_et_alimentationDMP_v1.0.0_20160323 (1).pdf` — guide historique MSSante / DMP (2016).
- `docs/Referentiel/MSSANTE/ANS_MSS_Manuel_d'utilisation_Outil_de_test_editeurs_MOTCO2_publique_20231220_v1.0-rwceJ8RA.pdf` — manuel de l'outil de test éditeurs MOTCO2.

#### Sources internes

- `CLAUDE.md` — règles de la forge (test-first, vérification locale, HAG, US-complete, polyrepo).
- Tasks `archived-task-001.md` à `archived-task-024.md`, `done-task-026.md`, `done-task-027.md`, `done-task-029.md`, `done-task-031.md`, `done-task-032.md`, `done-task-032bis-fhir-mock.md`, `done-task-032ter-greenmail-fixture.md`, `done-task-032quater-cda-samples.md`, `done-task-033.md`, `done-task-034.md`, `done-task-015a-suppression-detection-backend.md`, `done-task-015b-suppression-decision-endpoint-ui.md`, `done-task-015c-version-navigation.md`, `done-task-036-fix-version-navigation-routing.md`, `done-task-035.md`, `done-task-028.md`, `done-task-037.md`, `done-task-038-fix-mss-imap-test-tls.md`, `archived-task-048-psc-kc-identity-crosscheck.md`, `archived-task-049-backend-auth-optin-msssub-mssrpps.md` (livré hors workspace dans le proxy Keycloak via WindSurf), `archived-task-050-keycloak-protocol-mappers-msssub-mssrpps.md` (livré hors automation, realm Keycloak via WindSurf), `done-task-054-opt-in-remove-rpps-field.md` — apports incrémentaux à l'EPIC. Les 3 sub-tasks du découpage Option B 2026-05-07 (`task-032ter`, `task-032quater`, `task-032bis-fhir-mock`) sont toutes livrées. Task en cours sur le chantier sécurité PSC/KC ouvert par task-048 : `todo-task-052-angular-psc-token-refresh-on-identity-change.md` (defense-in-depth Angular — invalidation tokenAggregate.pscAccessToken sur changement d'identité, à shipper en complément des tasks 048/049/050).

---

*Documentation générée et maintenue par `/tech-writer` (cf. `agents/technical-writer.md`). Les entrées de l'historique des changelogs sont **append-only** entre les passes. Pour reconstruire intégralement ce fichier : `/tech-writer E009 --refresh`.*
